# 媒介达人一体化工作台功能实现方案

> 本文档已被新版整合方案替代。后续以 `达人媒介一体化前后端整合方案.md` 为准。

新版方案明确：媒介和达人筛选本质上是同一个岗位的连续工作，应统一为“达人媒介工作台”，前端和后端都按一个工作台、一套数据模型、一套 API 设计。

## 目标

媒介工作台和达人筛选工作台存在明显重合：都围绕达人资源、项目达人池、状态流转、飞书写回、项目进度和效率指标展开。

因此不建议把它们做成两套独立系统。建议融合为一套底层能力：

```txt
项目达人协作模块 Creator Collaboration
```

前端可以保留两个角色入口：

- 达人筛选入口：偏采集、评分、初筛、审号、入池。
- 媒介入口：偏建联、报价、档期、合作推进、投放追踪、预算优化。

但底层必须共用：

- 同一个 `project_id`
- 同一张项目达人池
- 同一个达人状态机
- 同一套达人详情
- 同一套沟通/操作日志
- 同一套飞书绑定和写回
- 同一套管理层指标

## 为什么要融合

### 重合点

| 功能 | 达人筛选 | 媒介 | 融合方式 |
|---|---|---|---|
| 达人列表 | 候选达人、已通过、备选 | 待建联、洽谈中、已合作 | 同一张项目达人池，不重复建表 |
| 达人详情 | 评分、标签、内容样本 | 报价、档期、沟通记录 | 同一个达人详情，按角色展示不同区块 |
| 状态流转 | 待筛、通过、淘汰、备选 | 建联、洽谈、签约、发稿 | 合并为完整状态机 |
| 风险标签 | 账号风险、内容风险 | 合作风险、预算风险 | 同一套风险标签体系 |
| 飞书写回 | 筛选结果写回 | 建联进度写回 | 同一套字段映射，按字段权限写回 |
| 项目进度 | 达人池完成度 | 合作推进度 | 统一进入项目健康度 |
| 管理指标 | 通过率、淘汰率 | 确认率、响应时长 | 统一进入 `ProjectMetrics` |

### 不融合的风险

- 同一个达人会在两个工作台出现两份状态。
- 媒介需要重新维护达人资产，筛选结果无法自然延续。
- 管理层看到的达人进度会被拆成两套指标。
- 飞书写回容易字段冲突。
- 后续项目复盘无法还原完整达人链路。

## 融合后的职责边界

融合不是取消角色，而是统一底层流程后按角色分工。

| 阶段 | 主责角色 | 辅助角色 | 说明 |
|---|---|---|---|
| 筛选标准 | 策划 / 达人筛选 | 媒介 | 策划给画像，达人筛选转成标准，媒介补充合作经验 |
| 达人采集 | 达人筛选 | 媒介 | 采集候选达人 |
| 初筛评分 | 达人筛选 | 媒介可查看 | 评分、硬性条件、淘汰原因 |
| 人工审号 | 达人筛选 | 媒介可标记关注 | 判断账号质量和内容风险 |
| 入池分层 | 达人筛选 | 媒介 | 通过、备选、风险、淘汰 |
| 建联沟通 | 媒介 | 达人筛选可查看 | 联系达人、记录反馈 |
| 报价档期 | 媒介 | 执行可查看 | 维护报价、档期、预算占用 |
| 合作确认 | 媒介 | 执行 | 确认后生成执行任务或交接 |
| 发稿回链 | 媒介 / 执行 | 管理层可查看 | 跟进交付节点 |
| 数据回流 | 媒介 / 执行 | 管理层 | 记录曝光、互动、成本、ROI |

## 统一状态机

建议用一个完整状态机覆盖达人从候选到复盘的生命周期：

```txt
候选待采集
  -> 已采集待评分
  -> 待人工筛选
  -> 待审号
  -> 已入池待建联
  -> 建联中
  -> 洽谈中
  -> 已确认合作
  -> 已排期
  -> 已发稿
  -> 已回链
  -> 已结案

任意阶段可进入：
  -> 已淘汰
  -> 备选
  -> 风险暂停
  -> 需复核
```

### 状态字段建议

```json
{
  "project_id": "project_xxx",
  "creator_id": "creator_xxx",
  "pipeline_stage": "contacting",
  "screening_status": "passed",
  "media_status": "negotiating",
  "owner_role": "media",
  "owner_user": "user_xxx",
  "last_action_at": "2026-05-12T10:00:00+08:00",
  "blocked_reason": "",
  "risk_tags": []
}
```

说明：

- `pipeline_stage` 是统一主状态。
- `screening_status` 保留筛选视角状态。
- `media_status` 保留媒介视角状态。
- 页面展示以 `pipeline_stage` 为准，角色页面可使用自己的辅助状态筛选。

## 统一数据模型

建议把达人相关数据抽成项目级实体：

```txt
ProjectCreator
  -> CreatorProfile
  -> ScreeningResult
  -> MediaContact
  -> CooperationSchedule
  -> DeliveryTracking
  -> PerformanceMetrics
  -> CreatorActivityLog
```

### ProjectCreator

项目内达人主记录。

核心字段：

- `project_id`
- `creator_id`
- `creator_name`
- `platform`
- `profile_url`
- `pipeline_stage`
- `owner_role`
- `owner_user`
- `score`
- `recommendation_level`
- `risk_tags`
- `created_at`
- `updated_at`

### ScreeningResult

达人筛选结果。

核心字段：

- `screening_status`
- `base_score`
- `dimension_scores`
- `matched_signals`
- `reject_reason`
- `review_comment`
- `reviewed_by`
- `reviewed_at`

### MediaContact

媒介沟通和商务信息。

核心字段：

- `contact_status`
- `contact_owner`
- `last_contact_at`
- `contact_logs`
- `quoted_price`
- `negotiated_price`
- `available_schedule`
- `cooperation_intent`

### DeliveryTracking

合作交付信息。

核心字段：

- `signed_at`
- `scheduled_publish_at`
- `published_at`
- `content_url`
- `backlink_url`
- `delivery_status`
- `abnormal_notes`

### PerformanceMetrics

投放表现数据。

核心字段：

- `exposure`
- `read_count`
- `interaction_count`
- `click_count`
- `conversion_count`
- `cpe`
- `cpm`
- `roi`
- `data_updated_at`

## 统一前端模块

建议新增共享模块：

```txt
ad-workbench/src/shared/creator-collaboration/
  CreatorPipelineBoard.jsx
  CreatorPipelineTable.jsx
  CreatorDetailDrawer.jsx
  CreatorStatusControls.jsx
  CreatorContactPanel.jsx
  CreatorScreeningPanel.jsx
  CreatorDeliveryPanel.jsx
  CreatorPerformancePanel.jsx
  CreatorRiskTags.jsx
  creatorPipelineApi.js
  creatorPipelineStatus.js
```

### 角色入口复用方式

达人筛选入口展示：

- 筛选标准
- 采集配置
- 初筛评分
- 人工筛选
- 审号
- 入池

媒介入口展示：

- 待建联达人
- 沟通记录
- 报价档期
- 合作确认
- 发稿回链
- 投放数据
- 预算表现

两者共用：

- `CreatorPipelineTable`
- `CreatorDetailDrawer`
- `CreatorStatusControls`
- `CreatorRiskTags`
- `creatorPipelineApi`

## 页面融合方案

### 方案 A：保留两个入口，共用底层模块

推荐第一阶段采用。

```txt
/workbench/screening
  展示达人协作模块的筛选视角

/workbench/media
  展示达人协作模块的媒介视角
```

优点：

- 不破坏现有路由和用户习惯。
- 权限边界清晰。
- 可逐步替换现有页面。

### 方案 B：合并为一个“达人协作工作台”

后续可考虑。

```txt
/workbench/creator-collaboration
```

内部按角色和阶段切换：

- 采集筛选
- 审号入池
- 建联沟通
- 合作交付
- 数据回流
- 达人资产

优点：

- 产品结构更清晰。
- 达人链路最完整。

风险：

- 需要调整导航和权限。
- 改动现有用户路径较大。

## 推荐导航调整

### 达人筛选入口

保留：

- 项目预览
- 采集工作台
- 筛选工作台
- 审号工作台
- 项目达人池
- 标准与飞书
- 操作日志

但“项目达人池”应改为统一达人池的筛选视角。

### 媒介入口

建议调整为：

- 工作概览
- 达人池建联
- 合作进度
- 发稿回链
- 投放追踪
- 预算表现
- 达人资产
- 报告中心

其中“达人池建联”直接读取达人筛选后的统一达人池，不再维护单独达人列表。

## 交接机制调整

原设计中有“达人筛选 -> 媒介”的硬交接。融合后建议改成状态移交：

```txt
已入池待建联
  owner_role: screening -> media
```

也就是说：

- 不再复制一份交接数据给媒介。
- 达人记录本身进入媒介阶段。
- 如需通知媒介，创建轻量 `ProjectHandoff` 或 `ProjectTimeline` 事件。

### 交接事件示例

```json
{
  "project_id": "project_xxx",
  "from_role": "screening",
  "to_role": "media",
  "title": "12 位达人已入池待建联",
  "payload": {
    "creator_ids": ["c1", "c2"],
    "next_stage": "ready_for_contact"
  }
}
```

## 接口融合建议

第一阶段复用现有达人筛选接口：

```txt
GET    /api/projects/{project_id}/creator-pool
PATCH  /api/projects/{project_id}/creator-pool/{creator_id}/stage
POST   /api/projects/{project_id}/creator-pool/{creator_id}/update-metrics
```

新增媒介字段更新接口时，不要另建媒介达人表，建议扩展项目达人池：

```txt
PATCH /api/projects/{project_id}/creator-pool/{creator_id}/contact
POST  /api/projects/{project_id}/creator-pool/{creator_id}/contact-logs
PATCH /api/projects/{project_id}/creator-pool/{creator_id}/delivery
PATCH /api/projects/{project_id}/creator-pool/{creator_id}/performance
```

后续可统一为：

```txt
GET    /api/projects/{project_id}/creators/pipeline
PATCH  /api/projects/{project_id}/creators/pipeline/{creator_id}
POST   /api/projects/{project_id}/creators/pipeline/{creator_id}/logs
POST   /api/projects/{project_id}/creators/pipeline/batch-update
```

## 飞书融合建议

飞书不应按工作台各绑一套。建议一个项目一套飞书绑定，按字段组写回：

```txt
基础字段：达人昵称、平台、链接、粉丝数
筛选字段：评分、审核状态、淘汰原因、推荐理由
媒介字段：建联状态、报价、档期、沟通备注
交付字段：发稿日期、内容链接、回链状态
数据字段：曝光、互动、CPE、ROI
```

字段权限：

- 达人筛选可写筛选字段。
- 媒介可写建联、交付、数据字段。
- 管理层只读。
- 管理员配置字段映射。

## 管理层指标融合

管理层不应分别看“达人筛选进度”和“媒介达人进度”，而应看完整达人链路：

- 候选达人数量。
- 已评分数量。
- 初筛通过率。
- 入池数量。
- 建联响应率。
- 合作确认率。
- 发稿完成率。
- 数据回流率。
- 单达人平均成本。
- 达人 ROI。
- 风险达人占比。

## 落地顺序

### 第一步：统一文档和术语

- 媒介和达人筛选统一称为“达人协作链路”。
- 项目达人池作为唯一达人资源池。
- 媒介不再新建独立达人资产表。

### 第二步：拆分 ScreeningDashboard

- 先按 `ScreeningDashboard拆分计划.md` 拆出达人池、飞书、日志、工具函数。
- 抽出 `CreatorPipeline` 相关组件和 API。

### 第三步：媒介入口接入项目达人池

- 媒介“达人建联”读取 `/creator-pool`。
- 在统一达人详情中补充建联、报价、档期字段。
- 状态更新仍写回同一条达人记录。

### 第四步：统一状态和日志

- 将筛选状态和媒介状态合并到 `pipeline_stage`。
- 所有状态变化写入 `ProjectTimeline`。

### 第五步：统一飞书和指标

- 飞书字段按字段组写回。
- 管理层以完整达人链路计算效率指标。

## 验收标准

- 同一个项目达人只存在一条主记录。
- 达人筛选入口和媒介入口看到的是同一批达人。
- 筛选通过后，达人自动进入“已入池待建联”。
- 媒介更新建联、报价、档期后，达人筛选入口也能看到状态变化。
- 飞书只需要绑定一次，筛选和媒介字段都能写回。
- 管理层能看到从采集、筛选、建联、合作到数据回流的完整漏斗。
