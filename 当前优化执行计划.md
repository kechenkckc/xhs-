# 当前优化执行计划

## 1. 执行目标

本计划用于把当前“有道答疑笔5-6月合作”筛选工作台，从单项目筛号 MVP 升级为可长期使用的达人资产系统。

近期目标：

- 筛选结果可长期沉淀。
- 达人池从前端展示升级为后端资产池。
- 飞书支持稳定写入、读取和同步。
- 规则评分与 LLM 匹配分析标准化。
- 支持后续数据更新、导出和复盘。

## 2. 总体执行路径

```text
第一阶段：数据底座重构
-> 第二阶段：项目达人池后端化
-> 第三阶段：飞书双向同步
-> 第四阶段：评分与 LLM 分析标准化
-> 第五阶段：采集与更新链路增强
-> 第六阶段：本地导出、投后复盘、媒介建联
```

## 3. 第一阶段：数据底座重构

### 目标

把达人从“项目列表数据”升级为“全局达人资产 + 项目关系”的长期数据模型。

### 任务

1. 新增全局达人表。

建议表名：

```text
creators_global
```

核心字段：

- creator_id
- pgy_url
- xiaohongshu_id
- pgy_blogger_id
- nickname
- creator_type
- persona_tags
- ip_city
- profile_url
- avatar_url
- created_at
- updated_at

2. 新增项目达人关系表。

建议表名：

```text
project_creators
```

核心字段：

- project_id
- creator_id
- pool_stage
- review_status
- review_reason
- reviewer
- reviewed_at
- total_score
- tier
- portfolio_role
- owner
- note
- created_at
- updated_at

3. 新增当前指标表。

建议表名：

```text
creator_metrics_current
```

核心字段：

- creator_id
- followers_count
- quote_price
- natural_cpc
- natural_cpe
- search_recommend_ratio
- fans_35_plus_ratio
- traffic_stability
- rate_limit_risk
- cost_30d
- cost_90d
- collected_at

4. 新增历史快照表。

建议表名：

```text
creator_metrics_history
```

每次采集、补全、更新都插入一条，不覆盖旧数据。

5. 新增状态流转日志。

建议表名：

```text
project_creator_stage_logs
```

记录达人从待审核、通过、备选、淘汰、待建联、合作中等状态的流转历史。

### 产出

- 数据库 schema 升级。
- 旧数据迁移脚本。
- `review_creator`、`upsert_creator`、`list_creators` 等函数适配新模型。

### 验收

- 同一达人可进入多个项目，但全局身份只保留一份。
- 项目内审核状态刷新后不丢失。
- 同一达人多次更新会产生历史快照。
- 项目达人池能按状态查询。

## 4. 第二阶段：项目达人池后端化

### 目标

让项目达人池页面不再依赖前端临时分组，而是从后端聚合接口读取。

### 任务

1. 新增达人池聚合接口。

```text
GET /api/projects/{project_id}/creator-pool
```

返回结构：

```json
{
  "project": {},
  "groups": {
    "已合作跟进中": [],
    "合格达人待合作": [],
    "待建联达人": [],
    "观察暂缓": []
  },
  "stats": {},
  "updated_at": ""
}
```

2. 新增单达人详情接口。

```text
GET /api/projects/{project_id}/creator-pool/{creator_id}
```

返回：

- 基础资料
- 项目状态
- 当前指标
- 历史快照
- 审核记录
- 飞书同步状态
- 建联记录

3. 新增状态变更接口。

```text
PATCH /api/projects/{project_id}/creator-pool/{creator_id}/stage
```

4. 前端项目达人池改为读后端接口。

### 产出

- 后端聚合接口。
- 前端项目达人池页面接入后端。
- 状态变更后自动刷新对应分组。

### 验收

- 刷新页面后达人仍在正确分组。
- 单个/批量通过后进入达人池。
- 淘汰后进入观察暂缓。
- 备选不再回到筛选工作台。

## 5. 第三阶段：飞书双向同步

### 目标

把飞书从“手动写回表格”升级为“团队协作镜像”，支持写入、读取、同步状态记录和冲突处理。

### 任务

1. 新增飞书同步状态表。

建议表名：

```text
feishu_sync_state
```

核心字段：

- project_id
- creator_id
- table_id
- feishu_record_id
- last_synced_at
- sync_direction
- sync_status
- last_error

2. 写入飞书改为 upsert。

逻辑：

- 有 `feishu_record_id`：更新记录。
- 没有 `feishu_record_id`：用蒲公英链接、达人 ID 查重。
- 查到：更新并保存 record_id。
- 查不到：新增并保存 record_id。

3. 增加读取飞书接口。

```text
POST /api/projects/feishu/pull
```

同步字段：

- 负责人
- 备注
- 建联状态
- 合作状态
- 档期
- 商务确认信息

4. 增加同步日志。

每次写入/读取记录：

- 成功条数
- 失败条数
- 失败原因
- 更新时间

### 产出

- 飞书 upsert 写入。
- 飞书读取合并。
- 同步状态表。
- 同步日志展示。

### 验收

- 同一达人多次写飞书不会重复新增。
- 飞书人工修改备注后可同步回本地。
- 同步失败能看到具体原因。
- 飞书字段映射缺失时给出可操作提示。

## 6. 第四阶段：评分与 LLM 分析标准化

### 目标

让评分可解释、可复核、可稳定迭代。

### 任务

1. 规则评分配置化。

针对有道答疑笔项目，先落地以下规则：

- 报价小于 20000。
- 35 岁以上粉丝占比大于 40%。
- CPC 小于 2。
- CPE 小于 20，低于 10 加分。
- 搜索+推荐占比大于 40%。
- 限流风险为低或无明显。
- 发布时间在 2026-05-07 至 2026-05-19。
- 30/90 天外溢进店成本低于 50。

2. LLM 输出固定 JSON。

建议字段：

```json
{
  "persona_match": 0,
  "audience_match": 0,
  "product_scene_match": 0,
  "topic_value": 0,
  "portfolio_role": "",
  "semantic_risk": "",
  "evidence": [],
  "concerns": [],
  "recommended_action": "",
  "summary": ""
}
```

3. 后端统一计算总分。

建议：

```text
总分 = 规则评分 65 + LLM 匹配 35
```

4. 新增评分版本和反馈表。

建议表：

```text
score_runs
score_feedback
```

### 产出

- 标准化评分模块。
- LLM 结构化分析结果入库。
- 前端展示匹配证据和风险顾虑。

### 验收

- 同一达人评分可复现。
- 每个分数有依据。
- 人工审核能看到 LLM 证据。
- 规则权重可按项目调整。

## 7. 第五阶段：采集与更新链路增强

### 目标

让系统支持持续采集、补全和更新，而不是单次导入。

### 任务

1. 蒲公英后台初筛采集增强。

- 支持采集批次。
- 支持字段完整度统计。
- 支持失败重试。

2. 蒲公英链接详情补全。

```text
POST /api/projects/{project_id}/creators/enrich
```

用于已有蒲公英链接批量补全详情。

3. 小红书前端关键词补充筛选。

先设计接口占位：

```text
POST /api/xhs/search-creators
```

后续接第三方 API 或爬虫服务。

4. 合作中达人数据更新。

```text
POST /api/projects/{project_id}/creator-pool/{creator_id}/update-metrics
```

更新后：

- 插入历史快照。
- 更新当前指标。
- 计算变动摘要。
- 可选同步飞书。

### 产出

- 采集批次页面。
- 达人详情补全接口。
- 合作中达人更新接口。
- 历史数据变化展示。

### 验收

- 每次采集有批次记录。
- 同一达人重复采集不会重复入库。
- 更新后能看到粉丝、报价、互动数据变化。
- 刷新后变化记录仍存在。

## 8. 第六阶段：导出、投后复盘、媒介建联

### 目标

补齐长期协作和项目复盘能力。

### 任务

1. 本地快照导出。

```text
GET /api/projects/{project_id}/exports/creator-pool.csv
GET /api/projects/{project_id}/exports/creator-pool.xlsx
```

保存目录：

```text
runtime/exports/{project_id}/
```

2. 投后复盘表。

建议表：

```text
campaign_posts
post_performance_snapshots
```

支持录入合作笔记链接，定期抓取数据。

3. 媒介建联表。

建议表：

```text
creator_outreach_records
creator_business_info
```

支持：

- 建联状态
- 话术模板
- 底价
- 返点
- 档期
- 寄样地址
- 合作意向

### 产出

- 导出能力。
- 投后复盘数据模型。
- 媒介建联工作流雏形。

### 验收

- 可导出当前项目达人池快照。
- 可录入合作笔记并追踪投后表现。
- 可记录达人建联与商务信息。

## 9. 推荐排期

### 第 1 周

- 数据表重构设计。
- 完成全局达人表和项目关系表。
- 完成旧数据迁移。
- 修复项目达人池后端聚合接口。

### 第 2 周

- 前端项目达人池接后端。
- 完成状态机和流转日志。
- 完成合作中达人更新记录持久化。

### 第 3 周

- 飞书 upsert 写入。
- 飞书 record_id 映射。
- 飞书读取合并人工字段。
- 同步日志展示。

### 第 4 周

- 规则评分配置化。
- LLM 匹配 JSON 标准化。
- 评分结果和证据展示。
- 本地 CSV/XLSX 快照导出。

### 后续阶段

- 小红书前端关键词补充筛选。
- 投后数据追踪。
- 媒介建联流程。
- 老达人定期巡检。

## 10. 风险与注意事项

### 数据模型风险

不要继续把 `creators` 当作全局达人和项目达人关系的混合表，否则后续多项目会难以维护。

### 飞书同步风险

不要使用简单 append 写入飞书，否则会产生大量重复达人。

必须实现 upsert 和 record_id 映射。

### LLM 风险

不要让 LLM 直接决定最终分数。

LLM 应只负责语义匹配和证据解释，最终分数由后端按规则计算。

### 采集风险

蒲公英和小红书采集都可能遇到登录、限流、字段缺失和页面变化。

必须保留批次、失败原因和重试能力。

## 11. 最近可执行的最小闭环

如果只做一个最小可用版本，建议优先完成：

1. `project_creators` 项目关系表。
2. `creator_metrics_history` 历史快照表。
3. 项目达人池后端聚合接口。
4. 飞书 upsert 写入。
5. 合作中达人数据更新持久化。
6. 本地 CSV 快照导出。

完成这六项后，当前项目就能从“页面可操作”升级为“数据可沉淀、可同步、可复用”的系统。

