# 策划工作台功能实现方案

## 定位

策划工作台负责项目早期的信息理解、策略产出和交接。它的核心不是单独生成方案，而是把策划产出结构化为后续执行、媒介、达人筛选可以直接使用的项目输入。

策划工作台必须建立在共享模块之上：

- `ProjectContext`：读取和切换当前项目。
- `ProjectHandoff`：把策划产出交接给执行、媒介、达人筛选。
- `ProjectTimeline`：记录 Brief 更新、策略生成、交接提交等动作。
- `ProjectAssetPool`：沉淀 Brief、策略文档、竞品洞察、社媒数据。
- `IntegrationBinding`：同步飞书文档、表格或外部资料。

## 当前页面基础

现有策划工作台包含以下 tab：

- 工作概览
- 舆情洞察
- Brief 中心
- 创意策略
- 社媒看板
- 项目中心
- Agent 中心
- 报告中心

第一阶段保留现有导航和交互，在其上接入项目级共享模块。

## 功能模块

### 1. 工作概览

#### 功能目标

让策划看到当前项目下与自己相关的待办、产出状态和交接状态。

#### 页面内容

- 当前项目卡片：项目名称、品牌、阶段、周期、预算、目标。
- 策划待办：待完善 Brief、待生成策略、待提交交接、被退回交接。
- 产出进度：Brief 完整度、洞察完成度、策略完成度、交接完成度。
- 最近动态：来自 `ProjectTimeline` 的策划相关日志。

#### 关键交互

- 切换项目后刷新所有数据。
- 点击待办跳转到对应 tab。
- 点击交接状态查看交接详情。

### 2. 舆情洞察

#### 功能目标

围绕当前项目收集和分析行业、竞品、用户讨论、平台趋势。

#### 页面内容

- 关键词监控。
- 平台声量趋势。
- 情绪分布。
- 竞品内容样本。
- 高互动话题。
- 可引用洞察卡片。

#### 产出

写入 `ProjectAssetPool`：

- `asset_type: insight`
- 关键词包
- 趋势图数据
- 用户痛点
- 竞品策略摘要
- 可引用证据链接

#### 共享模块关系

- 洞察保存后写入 `ProjectTimeline`。
- 洞察可被 Brief 中心引用。
- 洞察可进入策划到执行的交接单。

### 3. Brief 中心

#### 功能目标

把客户需求和策划理解整理成结构化 Brief，并作为项目后续流程的核心输入。

#### 表单字段建议

- 项目背景
- 产品/服务信息
- 核心卖点
- 目标人群
- 传播目标
- KPI
- 投放周期
- 预算范围
- 内容禁区
- 达人画像要求
- 平台要求
- 交付物要求
- 客户特殊要求

#### 状态

```txt
draft -> ready_for_strategy -> ready_for_handoff -> submitted -> returned
```

#### 关键交互

- 保存草稿。
- AI 辅助补全。
- 从舆情洞察引用证据。
- 生成达人筛选标准草案。
- 生成执行任务拆解草案。
- 提交给执行工作台。
- 提交给达人筛选工作台。

#### 共享模块关系

- Brief 保存到 `ProjectAssetPool`。
- Brief 提交生成 `ProjectHandoff`。
- Brief 变更写入 `ProjectTimeline`。

### 4. 创意策略

#### 功能目标

基于 Brief 产出内容方向、创意主题、达人内容脚本方向和平台策略。

#### 页面内容

- 策略方向列表。
- 内容主题矩阵。
- 平台打法建议。
- 达人内容角度。
- 风险与禁区提示。
- 可交付策略包。

#### 产出结构建议

```json
{
  "project_id": "project_xxx",
  "strategy_id": "strategy_xxx",
  "positioning": "",
  "core_message": "",
  "content_angles": [],
  "platform_plan": [],
  "creator_requirements": {},
  "execution_notes": [],
  "risk_notes": []
}
```

#### 下游流向

- 给执行：内容方向、交付物、时间节点。
- 给媒介：达人类型、预算、沟通口径。
- 给达人筛选：筛选标准、人群画像、平台要求。

### 5. 社媒看板

#### 功能目标

展示当前项目相关社媒数据，为策略和复盘提供依据。

#### 页面内容

- 平台声量。
- 互动率趋势。
- KOL 样本表现。
- 内容类型表现。
- 热词变化。

#### 共享模块关系

- 数据进入 `ProjectAssetPool`。
- 高价值洞察可一键加入 Brief。
- 关键变化写入 `ProjectTimeline`。

## 交接设计

### 策划到执行

交接内容：

- Brief 摘要
- 核心卖点
- 执行任务建议
- 交付物要求
- 时间节点
- 风险提示

状态：

```txt
pending -> accepted -> in_progress -> completed
pending -> returned
```

### 策划到达人筛选

交接内容：

- 达人画像
- 预算范围
- 平台要求
- 内容方向
- 硬性筛选条件
- 评分权重建议

达人筛选工作台接收后，可生成或更新项目筛选标准。

### 策划到媒介

交接内容：

- 达人沟通口径
- 合作要求
- 品牌禁区
- 预算和档期
- 候选达人类型

## 接口建议

```txt
GET    /api/projects/{project_id}/planning/brief
POST   /api/projects/{project_id}/planning/brief
PATCH  /api/projects/{project_id}/planning/brief

GET    /api/projects/{project_id}/planning/insights
POST   /api/projects/{project_id}/planning/insights

GET    /api/projects/{project_id}/planning/strategies
POST   /api/projects/{project_id}/planning/strategies
PATCH  /api/projects/{project_id}/planning/strategies/{strategy_id}

POST   /api/projects/{project_id}/handoffs/planner-to-executor
POST   /api/projects/{project_id}/handoffs/planner-to-screening
POST   /api/projects/{project_id}/handoffs/planner-to-media
```

## 验收标准

- 切换项目后，策划工作台展示对应项目数据。
- Brief 可保存、编辑、提交。
- 创意策略可生成并保存为项目资产。
- 策划产出可以生成交接单。
- 交接单能被执行、媒介或达人筛选接收。
- 所有关键操作写入项目动态。
