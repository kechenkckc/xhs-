# 达人分析优化执行清单

## 1. 目标

本清单用于把当前项目的达人分析能力，从“泛教育达人评分”升级为“面向有道答疑笔的品牌找达人分析方案”。

本次优化的核心原则：

- 真实可采字段，直接用于自动评分
- 可规则抽取字段，用规则或结构化提取落库
- 无法稳定采集的真实平台字段，改为人工复核，不再交给模型猜测

本次明确结论：

- `search_recommend_ratio` 改为人工复核项，不再由模型判断
- `natural_cpc / natural_cpe` 改为有效成本口径，允许用图文/视频单价兜底
- `child_grade` 改为规则抽取字段
- `rate_limit_risk` 改为系统推断风险字段


## 2. 本次优化后的 5 大评分标准

后续前端风险提示、自动评分、模型解析、人工复核，统一只围绕以下 5 个标准展开：

1. 目标人群匹配
2. 产品场景匹配
3. 真实流量质量
4. 成本效率
5. 执行确定性


## 3. 字段处理总方案

### 3.1 真实字段，直接使用

用于第一阶段自动量化评分：

- `followers_count`
- `quote_price`
- `daily_read_median`
- `daily_interaction_median`
- `cooperation_read_median`
- `cooperation_interaction_median`
- `image_cpm`
- `video_cpm`
- `image_read_unit_price`
- `video_read_unit_price`
- `image_interaction_unit_price`
- `video_interaction_unit_price`
- `fans_35_plus_ratio`
- `fans_35_44_ratio`
- `fans_44_plus_ratio`

### 3.2 可替代并立即落地

#### A. `natural_cpc`

统一改为“阅读单价”口径，取值优先级：

1. `natural_cpc`
2. `image_read_unit_price`
3. `video_read_unit_price`
4. `quote_price / 阅读参考值`

建议新增统一字段：

- `effective_cpc`
- `effective_cpc_source`

#### B. `natural_cpe`

统一改为“互动单价”口径，取值优先级：

1. `natural_cpe`
2. `image_interaction_unit_price`
3. `video_interaction_unit_price`

建议新增统一字段：

- `effective_cpe`
- `effective_cpe_source`

#### C. `fans_35_plus_ratio`

取值优先级：

1. 直接使用 `fans_35_plus_ratio`
2. 如果为空，则用 `fans_35_44_ratio + fans_44_plus_ratio`
3. 如果仍为空，则标记待补

建议新增：

- `fans_35_plus_ratio_source`

### 3.3 规则抽取字段

#### A. `child_grade`

从以下证据中抽取：

- `detail_text`
- `recent_notes[].title`
- `recent_notes[].content`
- `cooperation_note_cases[].title`
- `cooperation_note_cases[].content`
- `persona_tags`
- `topic_point`

标准化输出建议：

- `小学低年级`
- `小学高年级`
- `小升初`
- `初中`
- `高中`
- `多学段`
- `待补`

建议新增：

- `child_grade`
- `child_grade_confidence`
- `child_grade_evidence`

#### B. 产品场景标签

为“有道答疑笔”增加结构化标签：

- `作业答疑`
- `错题讲解`
- `提分方法`
- `家长辅导`
- `学习规划`
- `学习机/答疑笔测评`
- `中高考/升学`

建议新增：

- `content_scene_tags`
- `content_scene_evidence`

#### C. 内容呈现方式标签

建议新增：

- `presentation_style_tags`

推荐标签：

- `清单型`
- `经验型`
- `老师讲解型`
- `测评对比型`
- `日常记录型`
- `情绪共鸣型`

### 3.4 系统推断风险字段

#### `rate_limit_risk`

不再当作平台原生字段，改为系统推断：

- `低风险`
- `中风险`
- `高风险`
- `待补`

判断依据：

- 合作阅读明显低于日常阅读
- 合作互动明显低于日常互动
- 最近笔记表现波动异常
- 粉丝增长异常
- 报价与表现失衡

建议新增：

- `rate_limit_risk`
- `rate_limit_risk_reason`
- `traffic_stability`

### 3.5 人工复核字段

#### `search_recommend_ratio`

该字段不再由模型判断，不参与自动推断。

保留为人工复核项：

- `search_recommend_ratio`
- `search_recommend_review_status`
- `search_recommend_review_note`

状态建议：

- `待复核`
- `已复核`
- `无法确认`

规则：

- 未复核前，不因该字段自动淘汰达人
- 未复核前，不因该字段自动加分
- 如果 Brief 明确要求该项，则前端显示为必看人工项


## 4. 执行拆解

## 4.1 第一阶段：统一字段口径

### 目标

先把现有字段含义统一，避免前后端对同一指标理解不一致。

### 任务

1. 统一成本字段展示口径
   - 前端不再突出显示 `natural_cpc / natural_cpe`
   - 改为 `阅读单价 / 互动单价`
   - 使用 `effective_cpc / effective_cpe` 作为统一读取口径

2. 统一 `fans_35_plus_ratio` 口径
   - 后端补算
   - 前端不区分原始值和补算值，只展示最终值

3. 明确 `search_recommend_ratio` 为人工复核字段
   - 从自动评分逻辑中移除
   - 从模型解析任务中移除

### 影响文件

- [rpa_mcp_sync/creator_store.py](/d:/第三事业部/rpa_mcp_sync/creator_store.py)
- [ad-workbench/src/pages/screening/utils/creatorMappers.js](/d:/第三事业部/ad-workbench/src/pages/screening/utils/creatorMappers.js)

### 验收标准

- 前端不再把 `natural_cpc / natural_cpe` 当成唯一来源
- `fans_35_plus_ratio` 空值明显下降
- `search_recommend_ratio` 不再进入自动打分


## 4.2 第二阶段：数据库字段补齐

### 目标

把规则抽取字段、人工复核字段、风险字段补进数据结构。

### 任务

1. 在 `creator_metrics_current`、`creator_metrics_history` 中补字段

建议新增：

- `effective_cpc`
- `effective_cpc_source`
- `effective_cpe`
- `effective_cpe_source`
- `fans_35_plus_ratio_source`
- `child_grade_confidence`
- `child_grade_evidence`
- `content_scene_tags`
- `content_scene_evidence`
- `presentation_style_tags`
- `rate_limit_risk_reason`
- `search_recommend_review_status`
- `search_recommend_review_note`

2. 如不方便大改表结构，至少先允许这些字段写入 `raw_payload`

### 影响文件

- [rpa_mcp_sync/creator_store.py](/d:/第三事业部/rpa_mcp_sync/creator_store.py)

### 验收标准

- 新字段可写入、可读取、可回传前端
- 历史快照不丢失新增字段


## 4.3 第三阶段：后端计算与抽取逻辑

### 目标

把“替代、抽取、推断”真正落到后端，不依赖前端临时拼接。

### 任务

1. 新增有效成本计算函数

要求：

- 优先读原始 `natural_cpc / natural_cpe`
- 原始为空时自动降级到图文/视频单价
- 返回数值和来源

2. 新增 `fans_35_plus_ratio` 补算函数

要求：

- 如果 `fans_35_plus_ratio` 为空，则自动尝试 `35-44 + 44+`
- 补算成功后写入最终值

3. 新增 `child_grade` 抽取函数

规则：

- 从标题、正文、详情文本中识别 `一年级/二年级/初一/初二/小升初/高一` 等关键词
- 做标准化映射
- 输出结果、证据、置信度

4. 新增 `rate_limit_risk` 推断函数

建议规则：

- 若 `cooperation_read_median / daily_read_median < 0.5`，记为高风险候选
- 若 `cooperation_interaction_median / daily_interaction_median < 0.5`，记为高风险候选
- 若多项同时命中，则定为 `高风险`
- 若只轻微偏低，则定为 `中风险`
- 数据不足则 `待补`

5. `search_recommend_ratio` 改为人工状态字段

要求：

- 后端不再对其做推断
- 默认 `search_recommend_review_status = 待复核`

### 影响文件

- [rpa_mcp_sync/creator_store.py](/d:/第三事业部/rpa_mcp_sync/creator_store.py)
- [rpa_mcp_sync/pgy_browser.py](/d:/第三事业部/rpa_mcp_sync/pgy_browser.py)

### 验收标准

- 单个达人详情补全后，可看到 `effective_cpc / effective_cpe`
- 高分达人可抽到 `child_grade` 及证据
- 风险字段不再长期为空
- `search_recommend_ratio` 不会被系统误填


## 4.4 第四阶段：评分逻辑重构

### 目标

把评分拆成“自动量化评分 + 详情分析评分 + 人工复核项”三层。

### 任务

1. 自动量化评分

只看：

- 目标人群匹配
- 真实流量质量
- 成本效率

主要依据：

- 粉丝年龄
- 阅读/互动中位数
- 合作表现
- 有效 CPC/CPE/CPM

2. 详情分析评分

只看：

- 产品场景匹配
- 执行确定性

主要依据：

- `child_grade`
- `content_scene_tags`
- `presentation_style_tags`
- 笔记标题/内容/评论证据

3. 人工复核层

只保留：

- `search_recommend_ratio`
- 特殊风险
- 品牌安全与规避项

### 影响文件

- [rpa_mcp_sync/creator_store.py](/d:/第三事业部/rpa_mcp_sync/creator_store.py)
- [rpa_mcp_sync/web.py](/d:/第三事业部/rpa_mcp_sync/web.py)

### 验收标准

- 自动评分结果不再依赖 `search_recommend_ratio`
- 高分达人不再仅因为流量强就获得高推荐
- 产品弱匹配达人会被内容层拉低


## 4.5 第五阶段：大模型提示词与输出结构重写

### 目标

让模型只做它擅长的内容理解，不做平台事实字段猜测。

### 任务

1. 从提示词中移除以下判断职责

- `search_recommend_ratio`
- 平台真实流量来源占比
- 平台事实型限流结论

2. 保留并强化以下职责

- 是否适合有道答疑笔
- 更适合的合作角色
- 内容是否覆盖答疑、错题、学习规划、家长辅导
- 内容呈现是否适合转化
- 孩子学段/年级线索
- 核心短板与证据

3. 输出结构改为结构化 JSON

建议输出：

- `score_by_dimension`
- `evidence_by_dimension`
- `core_shortcomings`
- `portfolio_role`
- `manual_review_items`

其中 `manual_review_items` 必须能包含：

- `搜索+推荐占比待人工复核`

### 影响文件

- [rpa_mcp_sync/creator_store.py](/d:/第三事业部/rpa_mcp_sync/creator_store.py)

### 验收标准

- 模型输出不再对 `search_recommend_ratio` 给出伪结论
- 模型解析更聚焦于产品场景匹配
- 审核意见展示更短、更可证据化


## 4.6 第六阶段：前端展示改造

### 目标

让达人详情页、审核页、推荐理由与新的字段口径完全一致。

### 任务

1. 指标展示改造

- `natural_cpc` 展示文案改为 `阅读单价`
- `natural_cpe` 展示文案改为 `互动单价`
- `search_recommend_ratio` 展示为 `搜索+推荐占比（人工复核）`

2. 达人详情页增加待复核区块

建议展示：

- 搜索+推荐占比：待复核 / 已复核 / 无法确认
- 流量风险：低 / 中 / 高 / 待补
- 年级判断：结果 + 证据 + 置信度

3. 风险提示统一映射到 5 大评分标准

不得再出现游离于 5 大评分标准之外的风险文案。

4. 大模型意见缩短

要求：

- 每条意见只对应 1 个评分标准
- 同时给出 1 条核心证据

### 影响文件

- [ad-workbench/src/pages/screening/utils/creatorMappers.js](/d:/第三事业部/ad-workbench/src/pages/screening/utils/creatorMappers.js)
- [ad-workbench/src/pages/screening/components/screening-review/ScreeningReviewTab.jsx](/d:/第三事业部/ad-workbench/src/pages/screening/components/screening-review/ScreeningReviewTab.jsx)
- [ad-workbench/src/styles/index.css](/d:/第三事业部/ad-workbench/src/styles/index.css)

### 验收标准

- 详情页中能清楚区分“自动结论”和“待人工复核”
- 成本指标口径前后一致
- 风险提示全部落入 5 大标准


## 4.7 第七阶段：历史数据回填

### 目标

让现有 `youdao_001` 和后续项目都能吃到新规则，而不是只对新采数据生效。

### 任务

1. 对现有达人批量回填：

- `effective_cpc`
- `effective_cpe`
- `fans_35_plus_ratio`
- `child_grade`
- `rate_limit_risk`
- `search_recommend_review_status`

2. 重新跑一轮项目评分

重点观察：

- 高分达人排序是否变化
- 教育泛流量达人是否被下调
- 答疑笔场景更强的达人是否上升

### 影响文件

- [rpa_mcp_sync/creator_store.py](/d:/第三事业部/rpa_mcp_sync/creator_store.py)
- [tests/test_creator_store.py](/d:/第三事业部/tests/test_creator_store.py)
- [tests/test_web.py](/d:/第三事业部/tests/test_web.py)

### 验收标准

- 回填后核心空值率明显下降
- 高分结果更符合答疑笔场景


## 5. 开发优先级

### P0：本周必须完成

- `search_recommend_ratio` 改人工复核
- 成本口径改为 `effective_cpc / effective_cpe`
- `fans_35_plus_ratio` 自动补算
- 前端成本字段和待复核文案改造

### P1：紧接着完成

- `child_grade` 抽取与证据落库
- `rate_limit_risk` 推断逻辑
- 模型提示词去掉 `search_recommend_ratio` 判断

### P2：本轮收口项

- `content_scene_tags`
- `presentation_style_tags`
- 历史数据批量回填
- 重新评分与结果复核


## 6. 验收口径

本轮优化完成的判定标准：

1. 系统不再把 `search_recommend_ratio` 当模型字段或自动字段
2. 前端不再把 `natural_cpc / natural_cpe` 当唯一真实来源
3. 高分达人详情页能看到更清晰的年级、产品场景、短板证据
4. 风险提示只对应 5 大评分标准
5. 有道答疑笔的高分达人排序，更偏向真实产品适配，而不是泛教育流量大号


## 7. 建议执行顺序

建议按以下顺序推进，避免返工：

1. 字段口径统一
2. 数据库字段补齐
3. 后端计算与抽取逻辑
4. 自动评分重构
5. 模型提示词重写
6. 前端展示改造
7. 历史数据回填与复核


## 8. 本文档对应的当前关键文件

- [rpa_mcp_sync/creator_store.py](/d:/第三事业部/rpa_mcp_sync/creator_store.py)
- [rpa_mcp_sync/pgy_browser.py](/d:/第三事业部/rpa_mcp_sync/pgy_browser.py)
- [rpa_mcp_sync/web.py](/d:/第三事业部/rpa_mcp_sync/web.py)
- [ad-workbench/src/pages/screening/utils/creatorMappers.js](/d:/第三事业部/ad-workbench/src/pages/screening/utils/creatorMappers.js)
- [ad-workbench/src/pages/screening/components/screening-review/ScreeningReviewTab.jsx](/d:/第三事业部/ad-workbench/src/pages/screening/components/screening-review/ScreeningReviewTab.jsx)
- [ad-workbench/src/styles/index.css](/d:/第三事业部/ad-workbench/src/styles/index.css)
- [tests/test_creator_store.py](/d:/第三事业部/tests/test_creator_store.py)
- [tests/test_web.py](/d:/第三事业部/tests/test_web.py)
