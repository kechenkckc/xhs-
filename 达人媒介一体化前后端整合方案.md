# 达人媒介一体化前后端整合方案

## 核心判断

媒介和达人筛选本质上不是两个岗位，而是同一个岗位在项目不同阶段的连续工作。

原来拆成“达人筛选工作台”和“媒介工作台”，容易造成：

- 前端有两套达人列表。
- 后端有两套达人状态。
- 筛选结果需要交接给媒介，实际只是同一岗位继续处理。
- 飞书字段、达人状态、沟通记录和投放数据分散。
- 管理层无法看到完整达人链路。

因此建议统一为：

```txt
达人媒介工作台
```

它覆盖完整链路：

```txt
策划需求 -> 达人标准 -> 达人采集 -> 评分筛选 -> 人工审号 -> 达人入池 -> 建联沟通 -> 商务确认 -> 排期发稿 -> 数据回流 -> 达人复盘
```

## 整合目标

### 前端目标

- 合并媒介和达人筛选为一个工作台入口。
- 使用同一套达人池页面、达人详情、状态控件、飞书配置。
- 不再做“达人筛选到媒介”的交接页面。
- 页面内部按流程阶段分区，而不是按岗位分裂。

### 后端目标

- 合并达人筛选和媒介数据模型。
- 一个项目达人只存在一条项目级主记录。
- 筛选状态、建联状态、合作状态、投放数据都挂在同一条记录下。
- 对外提供一套统一 API。
- 管理层指标从同一套达人生命周期数据聚合。

## 产品结构

建议工作台命名：

```txt
达人媒介工作台
```

路由建议：

```txt
/workbench/creator-media
```

如果短期不调整路由，也可以先保留旧入口：

```txt
/workbench/screening
/workbench/media
```

但两个入口都应指向同一个底层模块，只是默认打开不同 tab。中长期建议收敛为一个入口。

## 前端导航设计

统一后的达人媒介工作台建议包含：

| Tab | 说明 |
|---|---|
| 工作概览 | 当前项目达人媒介整体进度、待办、风险 |
| 需求与标准 | 接收策划需求，维护达人画像、筛选标准、评分权重 |
| 达人采集 | 蒲公英采集、飞书导入、手动导入、采集批次 |
| 评分筛选 | 自动评分、硬性筛选、人工初筛、批量审核 |
| 审号评估 | 内容样本、账号质量、风险标签、深度判断 |
| 达人池 | 项目达人主池，覆盖所有阶段 |
| 建联商务 | 沟通记录、报价、档期、合作意向、确认合作 |
| 排期交付 | 签约、排期、发稿、回链、异常 |
| 数据回流 | 曝光、阅读、互动、转化、CPE、ROI |
| 飞书同步 | 项目级飞书绑定、字段映射、读写配置 |
| 操作日志 | 完整达人媒介动作记录 |
| 高级配置 | AI、采集、字段和规则配置 |

## 前端模块整合

### 建议目录

```txt
ad-workbench/src/pages/creator-media/
  CreatorMediaDashboard.jsx

  components/
    overview/
    requirement-standard/
    collection/
    screening/
    audit/
    creator-pool/
    contact/
    delivery/
    performance/
    feishu/
    logs/

  api/
    creatorMediaApi.js

  constants/
    creatorMediaStatus.js
    creatorMediaFields.js

  utils/
    creatorMediaMappers.js
    creatorMediaMetrics.js
```

### 从现有页面迁移

从 `ScreeningDashboard.jsx` 迁移：

- 项目达人池。
- 蒲公英采集。
- 筛选计划。
- 自动评分。
- 人工审核。
- 审号工作台。
- 飞书绑定和写回。
- 操作日志。

从 `MediaDashboard.jsx` 迁移：

- 达人建联。
- 项目进度。
- 广告追踪。
- 预算优化。
- 达人资产。

迁移后不保留两套实现。媒介相关能力作为达人生命周期后半段 tab 接入。

## 统一达人生命周期

建议使用一个主状态字段：

```txt
creator_stage
```

完整状态：

```txt
需求待确认
标准已生成
候选待采集
已采集待评分
待人工筛选
待审号
已入池
待建联
建联中
洽谈中
已确认合作
已签约
已排期
已发稿
已回链
数据已回流
已结案
```

任意阶段可进入：

```txt
已淘汰
备选
需复核
风险暂停
合作取消
```

### 辅助状态

为了方便页面筛选，可以保留辅助字段，但不能作为主流程割裂数据：

- `screening_status`
- `audit_status`
- `contact_status`
- `delivery_status`
- `performance_status`

主流程永远以 `creator_stage` 为准。

## 后端数据模型

### ProjectCreator

项目达人主表。一个项目内一个达人只有一条主记录。

字段建议：

```txt
project_id
creator_id
creator_name
platform
profile_url
source
creator_stage
owner_user
owner_role
priority
risk_level
risk_tags
created_at
updated_at
```

### CreatorScreening

筛选和评分信息。

```txt
project_id
creator_id
base_score
dimension_scores
hard_filter_result
matched_signals
screening_status
reject_reason
review_comment
reviewed_by
reviewed_at
```

### CreatorAudit

审号和内容质量评估。

```txt
project_id
creator_id
content_samples
account_quality
traffic_risk
brand_fit
audit_status
audit_comment
audited_by
audited_at
```

### CreatorContact

建联和商务信息。

```txt
project_id
creator_id
contact_status
contact_owner
contact_logs
quoted_price
negotiated_price
available_schedule
cooperation_intent
last_contact_at
```

### CreatorDelivery

排期和交付信息。

```txt
project_id
creator_id
contract_status
scheduled_publish_at
published_at
content_url
backlink_url
delivery_status
delivery_notes
```

### CreatorPerformance

投放和回流数据。

```txt
project_id
creator_id
exposure
read_count
interaction_count
click_count
conversion_count
cost
cpe
cpm
roi
data_source
data_updated_at
```

### CreatorActivityLog

达人维度动作日志。

```txt
project_id
creator_id
action_type
action
operator
detail
from_stage
to_stage
created_at
```

## 统一 API 设计

### 项目达人池

```txt
GET    /api/projects/{project_id}/creator-media/creators
POST   /api/projects/{project_id}/creator-media/creators
PATCH  /api/projects/{project_id}/creator-media/creators/{creator_id}
DELETE /api/projects/{project_id}/creator-media/creators/{creator_id}
```

### 状态推进

```txt
POST /api/projects/{project_id}/creator-media/creators/{creator_id}/stage
POST /api/projects/{project_id}/creator-media/creators/batch-stage
```

### 筛选评分

```txt
POST /api/projects/{project_id}/creator-media/collect
POST /api/projects/{project_id}/creator-media/score
POST /api/projects/{project_id}/creator-media/review
POST /api/projects/{project_id}/creator-media/audit
```

### 建联商务

```txt
PATCH /api/projects/{project_id}/creator-media/creators/{creator_id}/contact
POST  /api/projects/{project_id}/creator-media/creators/{creator_id}/contact-logs
PATCH /api/projects/{project_id}/creator-media/creators/{creator_id}/price
PATCH /api/projects/{project_id}/creator-media/creators/{creator_id}/schedule
```

### 交付和数据

```txt
PATCH /api/projects/{project_id}/creator-media/creators/{creator_id}/delivery
PATCH /api/projects/{project_id}/creator-media/creators/{creator_id}/performance
POST  /api/projects/{project_id}/creator-media/performance/import
```

### 飞书同步

```txt
GET  /api/projects/{project_id}/creator-media/feishu/connection
POST /api/projects/{project_id}/creator-media/feishu/connection
GET  /api/projects/{project_id}/creator-media/feishu/fields
POST /api/projects/{project_id}/creator-media/feishu/writeback
POST /api/projects/{project_id}/creator-media/feishu/pull
```

### 日志和指标

```txt
GET /api/projects/{project_id}/creator-media/logs
GET /api/projects/{project_id}/creator-media/metrics
```

## 与现有接口兼容

短期可以保留旧接口作为兼容层：

```txt
/api/projects/{project_id}/creators
/api/projects/{project_id}/creator-pool
/api/projects/{project_id}/creators/score
/api/projects/{project_id}/creators/review
/api/projects/feishu/*
```

但后端内部应逐步映射到统一模型。

兼容策略：

- 旧 `creator-pool` 读取统一 `ProjectCreator`。
- 旧 `creators/review` 更新 `CreatorScreening` 和 `creator_stage`。
- 旧飞书写回读取统一字段映射。
- 新页面逐步切换到 `/creator-media/*` API。

## 飞书字段整合

飞书只做项目级绑定，不按“筛选”和“媒介”拆两套。

字段分组：

| 字段组 | 示例字段 |
|---|---|
| 基础字段 | 达人昵称、平台、主页链接、粉丝数、类目 |
| 筛选字段 | 评分、审核状态、淘汰原因、推荐理由、风险标签 |
| 审号字段 | 内容样本、账号质量、限流风险、品牌匹配 |
| 建联字段 | 建联状态、沟通负责人、报价、档期、合作意向 |
| 交付字段 | 签约状态、排期、发稿链接、回链状态 |
| 数据字段 | 曝光、阅读、互动、成本、CPE、ROI |

权限：

- 达人媒介岗可写完整达人链路字段。
- 策划可查看筛选标准和达人画像。
- 执行可查看合作确认、排期和交付字段。
- 管理层只读。
- 管理员配置字段映射和 API。

## 与其他工作台的关系

### 策划到达人媒介

策划输出：

- 达人画像。
- 内容方向。
- 预算范围。
- 平台要求。
- 筛选硬性条件。
- 品牌禁区。

达人媒介接收后：

- 生成筛选标准。
- 生成采集条件。
- 进入采集和筛选流程。

### 达人媒介到执行

达人媒介输出：

- 已确认合作达人。
- 报价。
- 档期。
- 发稿节点。
- 交付要求。
- 注意事项。

执行接收后：

- 生成执行任务。
- 跟进交付物。
- 处理内容审核和异常。

### 达人媒介到管理层

管理层查看：

- 达人漏斗。
- 入池率。
- 建联响应率。
- 合作确认率。
- 发稿完成率。
- 数据回流率。
- 达人成本和 ROI。
- 风险达人占比。

## 管理层指标

统一后，管理层不再分别看“筛选效率”和“媒介效率”，而是看达人媒介完整效率。

建议指标：

- 候选达人数量。
- 已评分数量。
- 初筛通过率。
- 审号通过率。
- 入池数量。
- 建联响应率。
- 洽谈推进率。
- 合作确认率。
- 发稿完成率。
- 数据回流率。
- 平均单达人沟通时长。
- 平均单达人合作成本。
- 平均 CPE。
- 平均 ROI。
- 风险达人占比。

## 前端整合步骤

### 第一步：文档和术语统一

- 统一称为“达人媒介工作台”。
- 不再使用“达人筛选 -> 媒介交接”作为主流程。
- 统一使用“达人生命周期推进”。

### 第二步：先拆达人筛选大文件

按 `ScreeningDashboard拆分计划.md` 拆出：

- 采集。
- 评分。
- 审号。
- 达人池。
- 飞书。
- 日志。

拆分过程不改变功能。

### 第三步：把媒介功能接入统一达人池

从 `MediaDashboard.jsx` 抽出：

- 达人建联。
- 合作进度。
- 广告追踪。
- 预算优化。

接入统一 `ProjectCreator` 数据，而不是使用独立 mock 表。

### 第四步：收敛路由和导航

短期：

- `/workbench/screening` 默认打开采集筛选视角。
- `/workbench/media` 默认打开建联商务视角。
- 两者底层共用 `CreatorMediaDashboard`。

中期：

- 新增 `/workbench/creator-media`。
- 旧入口跳转或作为别名。

### 第五步：统一状态控件

所有达人状态更新都走统一控件：

- 推进阶段。
- 退回阶段。
- 标记风险。
- 标记备选。
- 标记淘汰。
- 批量更新。

## 后端整合步骤

### 第一步：建立统一模型

先在后端内部建立统一项目达人模型，即使数据库暂时沿用现有结构，也要通过服务层聚合成统一对象。

### 第二步：兼容旧接口

旧接口不立即删除，避免影响现有达人筛选工作台。

### 第三步：新增统一 API

新增 `/creator-media/*` API，前端新模块逐步接入。

### 第四步：迁移飞书读写

飞书写回改成字段组写回，不再按工作台拆分。

### 第五步：统一指标计算

管理层指标从统一达人生命周期计算。

## 不建议继续保留的设计

不建议：

- 媒介单独建一套达人资产表。
- 达人筛选向媒介复制达人列表。
- 两个工作台分别绑定飞书。
- 筛选状态和媒介状态互不关联。
- 管理层分别看两套达人进度。

建议：

- 一个项目达人池。
- 一个达人生命周期。
- 一个飞书绑定。
- 一套达人指标。
- 一个达人媒介工作台。

## 验收标准

- 前端只有一套达人池组件。
- 后端一个项目达人只有一条主记录。
- 筛选、审号、建联、交付、数据都能在同一个达人详情中查看。
- 达人从采集到结案能通过同一状态机推进。
- 飞书只需绑定一次，能同步完整达人链路字段。
- 策划能把达人需求交给达人媒介工作台。
- 执行能接收已确认合作达人的排期和交付要求。
- 管理层能看到完整达人漏斗和效率指标。
