# 超级管理员工作台功能实现方案

## 定位

超级管理员工作台负责系统配置、账号权限、Agent 编排、API 配置和审计日志。它服务所有工作台，重点保证多工作台共享模块的权限、安全和配置可控。

超级管理员工作台必须建立在共享模块之上：

- `ProjectContext`：配置项目可见范围。
- `IntegrationBinding`：管理系统级和项目级集成。
- `ProjectTimeline`：区分项目动态和系统审计。
- `ProjectMetrics`：提供系统运行指标。
- `RoleWorkspaceShell`：统一角色工作台入口配置。

## 当前页面基础

现有超级管理员工作台包含以下 tab：

- 系统概览
- 账号管理
- 权限配置
- Agent 编排
- API 配置
- 审计日志

第一阶段重点完善账号、权限、API 和审计对多工作台的支撑。

## 功能模块

### 1. 系统概览

#### 功能目标

展示系统运行状态和多工作台使用情况。

#### 页面内容

- 用户数。
- 活跃账号。
- 工作台访问量。
- Agent 运行状态。
- API 调用状态。
- 错误事件。
- 最近审计日志。

#### 指标来源

- 登录日志。
- API 调用日志。
- Agent 运行日志。
- 项目动态数量。

### 2. 账号管理

#### 功能目标

管理用户账号、岗位、系统角色和数据范围。

#### 用户字段

- 姓名。
- 邮箱。
- 手机。
- 业务岗位。
- 系统角色。
- 数据范围。
- 可访问项目。
- 状态。
- 最后登录时间。

#### 关键交互

- 新建账号。
- 编辑账号。
- 禁用/启用账号。
- 重置密码或授权方式。
- 设置岗位。
- 设置项目可见范围。

### 3. 权限配置

#### 功能目标

控制不同角色在多工作台中的访问和操作权限。

#### 权限维度

- 工作台访问权限。
- 页面 tab 权限。
- 项目数据范围。
- 操作权限。
- 审批权限。
- 集成配置权限。

#### 角色建议

```txt
planner
executor
media
screening
manager
admin
super_admin
```

#### 权限示例

| 权限 | 策划 | 执行 | 媒介 | 达人筛选 | 管理层 | 管理员 |
|---|---|---|---|---|---|---|
| 查看项目 | 是 | 是 | 是 | 是 | 是 | 是 |
| 编辑 Brief | 是 | 否 | 否 | 否 | 可查看 | 是 |
| 接收执行任务 | 否 | 是 | 否 | 否 | 可查看 | 是 |
| 更新达人建联 | 否 | 可查看 | 是 | 否 | 可查看 | 是 |
| 更新筛选标准 | 可提交 | 否 | 否 | 是 | 可查看 | 是 |
| 查看全局指标 | 否 | 否 | 否 | 否 | 是 | 是 |
| 系统配置 | 否 | 否 | 否 | 否 | 否 | 是 |

### 4. Agent 编排

#### 功能目标

管理各工作台使用的 Agent、触发条件、权限和运行日志。

#### Agent 类型

- Brief 生成 Agent。
- 舆情洞察 Agent。
- 执行日报 Agent。
- 达人评分 Agent。
- 飞书字段映射 Agent。
- 风险预警 Agent。
- 报告生成 Agent。

#### 配置字段

- Agent 名称。
- 所属工作台。
- 可用角色。
- 触发方式。
- 输入来源。
- 输出目标。
- 是否写入项目动态。
- 是否需要审批。

### 5. API 配置

#### 功能目标

统一管理 LLM、飞书、蒲公英、数据服务等外部 API 配置。

#### 配置范围

- 系统级配置。
- 项目级配置。
- 工作台级配置。

#### 关键交互

- 保存配置。
- 测试连接。
- 查看 key 配置状态。
- 切换 provider。
- 查看最近调用状态。

#### 与共享模块关系

- 飞书配置进入 `IntegrationBinding`。
- LLM 配置服务 Agent 编排。
- 配置变更写入系统审计日志。

### 6. 审计日志

#### 功能目标

追踪系统级敏感操作。

#### 与项目动态区别

项目动态记录业务协作过程，例如任务完成、交接提交、达人入池。

审计日志记录系统安全和配置动作，例如权限变更、账号禁用、API Key 更新、Agent 配置变更。

#### 审计字段

- 操作时间。
- 操作人。
- 操作类型。
- 模块。
- 操作对象。
- 详情。
- IP。
- 结果。

## 接口建议

```txt
GET    /api/admin/overview

GET    /api/admin/accounts
POST   /api/admin/accounts
PATCH  /api/admin/accounts/{user_id}
POST   /api/admin/accounts/{user_id}/disable
POST   /api/admin/accounts/{user_id}/enable

GET    /api/admin/permissions
POST   /api/admin/permissions
PATCH  /api/admin/permissions/{role_id}

GET    /api/admin/agents
POST   /api/admin/agents
PATCH  /api/admin/agents/{agent_id}
GET    /api/admin/agents/{agent_id}/runs

GET    /api/admin/integrations
POST   /api/admin/integrations
POST   /api/admin/integrations/test

GET    /api/admin/audit-logs
```

## 验收标准

- 能管理账号和岗位。
- 能配置每个角色可访问的工作台和项目范围。
- 能管理 Agent 的可用范围和运行状态。
- 能配置并测试系统 API。
- 敏感操作进入审计日志。
- 权限配置能影响各工作台入口和操作按钮。
