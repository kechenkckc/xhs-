# 达人筛选工作台功能实现方案

## 定位

达人筛选工作台后续不再作为独立岗位工作台建设。达人筛选能力应并入“达人媒介工作台”，作为达人生命周期的前半段能力：标准生成、采集、评分、人工筛选、审号和入池。

详细融合方案见：`达人媒介一体化前后端整合方案.md`。

达人筛选工作台必须建立在共享模块之上：

- `ProjectContext`：围绕当前项目运行。
- `ProjectHandoff`：接收策划交接的达人筛选要求；入池后通过状态移交通知媒介。
- `ProjectAssetPool`：沉淀候选达人、已通过达人、备选达人、建联状态和合作数据。
- `ProjectTimeline`：记录采集、评分、审核、入池、写回等动作。
- `IntegrationBinding`：管理飞书绑定、字段映射和写回。
- `ProjectMetrics`：输出筛选效率、通过率、淘汰原因、达人池完成度。

## 当前页面基础

现有达人筛选工作台包含以下 tab：

- 项目预览
- 采集工作台
- 筛选工作台
- 审号工作台
- 项目达人池
- 立项 + 标准 + 飞书绑定
- 操作日志
- 高级配置

当前工作台已经有较成熟的项目级闭环，应优先保留功能并完成拆分，再接入共享模块。

## 功能模块

### 1. 项目预览

#### 功能目标

展示达人筛选相关项目，并支持进入当前项目。

#### 页面能力

- 项目列表。
- 项目搜索。
- 项目状态筛选。
- 项目卡片进度。
- 归档、恢复、删除。
- 新建项目。

#### 共享模块关系

第一阶段使用现有项目接口，拆分稳定后并入 `ProjectHub`。

### 2. 采集工作台

#### 功能目标

基于项目筛选计划，执行蒲公英采集和候选达人入库。

#### 输入

- 项目 Brief。
- 策划交接的达人画像。
- 筛选标准。
- 蒲公英筛选条件。
- 飞书字段映射。

#### 输出

- 候选达人列表。
- 采集批次。
- 采集日志。
- 采集异常。

#### 关键交互

- 编辑采集条件。
- 保存筛选计划。
- 启动采集。
- 查看采集结果。
- 进入人工筛选。

### 3. 筛选工作台

#### 功能目标

对候选达人进行评分、筛选和审核。

#### 页面能力

- 达人列表。
- 搜索。
- 状态筛选。
- 分数筛选。
- 达人类型筛选。
- 排序。
- 单选和多选。
- 单人审核。
- 批量审核。
- 达人详情。
- 重新评分。

#### 审核状态

```txt
pending -> passed
pending -> rejected
pending -> backup
pending -> review
```

#### 共享模块关系

- 审核结果写入 `ProjectTimeline`。
- 通过和备选达人进入 `ProjectAssetPool`。
- 通过率和淘汰原因进入 `ProjectMetrics`。

### 4. 审号工作台

#### 功能目标

对重点达人进行更深层的账号质量判断，降低合作风险。

#### 页面内容

- 达人匹配度。
- 内容样本。
- 风险标签。
- 推荐理由。
- 数据完整度。
- 账号异常提示。

#### 关键交互

- 查看达人详情。
- 拉取详情。
- 重新评分。
- 通过、淘汰、备选、复核。
- 返回筛选工作台。

### 5. 项目达人池

#### 功能目标

沉淀项目达人资源，并作为媒介建联、合作推进、投放追踪的同一数据源。

#### 阶段建议

```txt
待人工筛选 -> 待审号 -> 已入池待建联 -> 建联中 -> 洽谈中 -> 已确认合作 -> 已排期 -> 已发稿 -> 已回链 -> 已结案

任意阶段可进入：
已淘汰 / 备选 / 风险暂停 / 需复核
```

#### 页面能力

- 按阶段查看达人。
- 标签筛选。
- 查看评分和推荐理由。
- 更新达人指标。
- 写回飞书。
- 导出 CSV/XLSX。
- 将达人状态推进到“已入池待建联”。
- 必要时创建轻量通知，让媒介入口出现待处理提醒。

#### 共享模块关系

- 达人池升级为 `ProjectAssetPool` 的 creator 资源。
- 媒介工作台直接消费同一批达人，不另建媒介达人表。
- 阶段变化写入 `ProjectTimeline`。

### 6. 立项 + 标准 + 飞书绑定

#### 功能目标

管理达人筛选项目的基础信息、筛选标准和飞书集成。

#### 页面能力

- 编辑项目基础信息。
- 生成量化筛选标准。
- 编辑硬性筛选条件。
- 编辑评分权重。
- 保存筛选计划。
- 飞书连接保存。
- 飞书连接测试。
- 读取表格。
- 读取字段。
- 字段映射展示。
- 写回飞书。

#### 共享模块关系

- 项目信息最终进入 `ProjectSettings`。
- 飞书部分最终进入 `IntegrationBinding`。
- 筛选标准作为达人筛选子流程配置保留。

### 7. 操作日志

#### 功能目标

查看达人筛选过程中的关键操作。

#### 事件类型

- 项目管理。
- 飞书操作。
- 采集任务。
- 筛选任务。
- 审核操作。
- 写回操作。

#### 共享模块关系

第一阶段保留现有 `AuditLogTab`，后续并入 `ProjectTimeline`。

### 8. 高级配置

#### 功能目标

管理达人筛选相关 AI 模型配置。

#### 共享模块关系

后续与超级管理员的 API 配置打通：

- 系统级配置由管理员维护。
- 项目级或工作台级配置在达人筛选中可查看或局部覆盖。

## 交接设计

### 策划到达人筛选

输入：

- 达人画像。
- 内容方向。
- 预算范围。
- 平台要求。
- 筛选硬性条件。
- 评分权重建议。

接收后动作：

- 更新项目筛选标准。
- 生成蒲公英采集条件。
- 进入采集工作台。

### 达人筛选到媒介

融合后不再复制一份达人池给媒介，而是把统一达人记录推进到媒介阶段。

状态移交：

```txt
pipeline_stage: 已入池待建联
owner_role: media
```

媒介入口读取同一个项目达人池，并继续补充建联、报价、档期、合作和投放数据。

## 接口建议

现有接口继续保留：

```txt
GET    /api/projects
GET    /api/projects/{project_id}
POST   /api/projects/{project_id}
GET    /api/projects/{project_id}/creators
POST   /api/projects/{project_id}/creators/import
POST   /api/projects/{project_id}/creators/score
POST   /api/projects/{project_id}/creators/review
GET    /api/projects/{project_id}/creator-pool
PATCH  /api/projects/{project_id}/creator-pool/{creator_id}/stage
POST   /api/projects/{project_id}/creator-pool/{creator_id}/update-metrics
GET    /api/projects/{project_id}/logs
POST   /api/projects/{project_id}/screening-standard/optimize
POST   /api/projects/feishu/connection
POST   /api/projects/feishu/test
GET    /api/projects/feishu/tables
GET    /api/projects/feishu/fields
POST   /api/projects/feishu/writeback
```

新增共享交接接口：

```txt
GET  /api/projects/{project_id}/handoffs?to_role=screening
POST /api/projects/{project_id}/handoffs/screening-to-media-notice
```

## 验收标准

- 现有达人筛选功能不受影响。
- 能接收策划交接并更新筛选标准。
- 采集、评分、人工筛选、审号、达人池流程可跑通。
- 达人池能作为统一项目达人池被媒介工作台直接消费。
- 媒介更新建联、报价、档期后，达人筛选入口也能看到状态变化。
- 飞书绑定和写回保持可用。
- 筛选效率和达人池完成度能进入管理层指标。
