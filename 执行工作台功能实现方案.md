# 执行工作台功能实现方案

## 定位

执行工作台负责承接策划交接，将项目目标拆解为任务，推动交付，处理异常，并把执行进度回流给管理层和项目总览。

执行工作台必须建立在共享模块之上：

- `ProjectContext`：围绕当前项目工作。
- `ProjectHandoff`：接收策划、媒介、达人筛选的交接。
- `ProjectTaskBoard`：任务拆解和进度推进。
- `ProjectTimeline`：记录任务状态变化和异常。
- `ProjectMetrics`：产出执行效率、延期率、完成率。
- `ProjectAssetPool`：管理交付物、素材、报告。

## 当前页面基础

现有执行工作台包含以下 tab：

- 工作概览
- 需求接入
- Agent 编排
- 任务看板
- Daily Push
- 执行跟进
- 项目中心
- 报告中心

第一阶段保留现有导航，在任务和交接上接入共享模块。

## 功能模块

### 1. 工作概览

#### 功能目标

展示当前项目下执行岗位的任务状态、交接状态和风险。

#### 页面内容

- 当前项目执行状态。
- 待接收交接。
- 今日到期任务。
- 已延期任务。
- 阻塞事项。
- 最近执行动态。

#### 指标

- 任务完成率。
- 平均处理时长。
- 延期任务数。
- 返工次数。
- 阻塞事项数量。

### 2. 需求接入

#### 功能目标

接收来自策划、媒介、达人筛选或管理层的项目需求。

#### 来源

- 策划交接单。
- 媒介确认的达人合作安排。
- 管理层新增要求。
- 客户临时需求。

#### 关键交互

- 查看交接详情。
- 接收交接。
- 退回交接并填写原因。
- 一键生成任务。
- 关联已有任务。

#### 数据关系

```txt
Handoff -> Requirement -> Task
```

### 3. Agent 编排

#### 功能目标

为执行任务配置自动化 Agent，例如日报生成、素材检查、数据抓取、异常提醒。

#### 页面内容

- Agent 列表。
- Agent 运行状态。
- 关联项目和任务。
- 最近运行日志。
- 手动触发。

#### 共享模块关系

- Agent 输出写入 `ProjectAssetPool`。
- 运行日志写入 `ProjectTimeline`。
- 异常进入 `ProjectMetrics` 风险项。

### 4. 任务看板

#### 功能目标

执行工作台的核心页面，用于管理项目任务流。

#### 看板列

```txt
待处理 -> 进行中 -> 待审核 -> 已完成
        -> 阻塞
        -> 退回
```

#### 任务字段

- 任务名称
- 所属项目
- 来源交接
- 负责人
- 协作人
- 截止时间
- 优先级
- 状态
- 交付物
- 阻塞原因
- 最近更新

#### 关键交互

- 新建任务。
- 从交接单生成任务。
- 拖拽或按钮更新状态。
- 分配负责人。
- 设置截止时间。
- 上传或关联交付物。
- 标记阻塞。
- 提交审核。
- 完成任务。

#### 数据回流

- 任务状态变化写入 `ProjectTimeline`。
- 任务完成率进入 `ProjectMetrics`。
- 交付物进入 `ProjectAssetPool`。

### 5. Daily Push

#### 功能目标

自动生成和推送项目执行日报。

#### 内容来源

- 今日完成任务。
- 今日新增任务。
- 延期任务。
- 阻塞事项。
- 明日重点。
- 需要管理层介入的问题。

#### 关键交互

- 预览日报。
- 编辑日报。
- 推送到飞书或其他渠道。
- 记录发送状态。

#### 共享模块关系

- 日报作为 `ProjectAssetPool` 的 report 资产。
- 推送动作写入 `ProjectTimeline`。

### 6. 执行跟进

#### 功能目标

跟进跨岗位事项和异常事项。

#### 类型

- 达人对接。
- 素材回收。
- 内容审核。
- 异常处理。
- 数据收集。
- 排期管理。
- 日常维护。

#### 关键交互

- 催办。
- 记录响应。
- 标记超时。
- 升级为风险。
- 通知管理层。

## 接口建议

```txt
GET    /api/projects/{project_id}/handoffs?to_role=executor
POST   /api/projects/{project_id}/handoffs/{handoff_id}/accept
POST   /api/projects/{project_id}/handoffs/{handoff_id}/return

GET    /api/projects/{project_id}/tasks
POST   /api/projects/{project_id}/tasks
PATCH  /api/projects/{project_id}/tasks/{task_id}
POST   /api/projects/{project_id}/tasks/from-handoff/{handoff_id}

GET    /api/projects/{project_id}/execution/daily-reports
POST   /api/projects/{project_id}/execution/daily-reports
POST   /api/projects/{project_id}/execution/daily-reports/{report_id}/push

GET    /api/projects/{project_id}/execution/followups
POST   /api/projects/{project_id}/execution/followups
PATCH  /api/projects/{project_id}/execution/followups/{followup_id}
```

## 验收标准

- 执行工作台能读取当前项目。
- 能接收策划交接并生成任务。
- 任务看板可完成状态流转。
- 任务状态变化能回流项目动态和管理层指标。
- Daily Push 能基于项目任务生成。
- 异常跟进能进入风险或阻塞状态。
