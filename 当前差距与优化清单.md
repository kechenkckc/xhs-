# 当前项目差距与优化清单

## 1. 当前实现概览

当前项目已经形成一个围绕“有道答疑笔5-6月合作”的达人筛选 MVP。

当前已有能力：

- 项目预览
- 采集工作台
- 筛选工作台
- 项目达人池
- 立项 + 标准 + 飞书绑定
- 操作日志
- 高级模型配置
- 蒲公英采集入口
- 本地 SQLite 数据库
- 规则评分和部分 LLM 配置能力
- 单个/批量人工筛选
- 飞书配置、字段读取和写回接口

当前系统已经覆盖“采集 -> 评分 -> 人工筛选 -> 项目达人池”的主干，但距离长期使用的完整协同系统仍有差距。

## 2. 业务流程当前状态

### 2.1 立项与标准

当前状态：

- 已有项目 brief。
- 已有筛选标准 CSV。
- 前端支持立项信息和量化标准展示。
- 支持 AI 优化量化标准入口。

问题：

- Brief 与筛选标准还没有完全产品化为稳定数据模型。
- 项目标准与飞书字段映射仍偏页面配置，没有形成版本化标准。
- 缺少项目标准变更历史。

优化建议：

- 增加 `project_screening_standards` 表。
- 每次标准变更写版本号。
- 保留 brief 原文、结构化 brief、筛选标准、字段映射结果。

## 3. 达人池数据模型优化

当前状态：

- 本地已有 `creators`、`creator_metrics`、`creator_scores`、`screening_reviews` 等表。
- 审核状态可以持久化。
- 项目达人池前端已有分组展示。

问题：

- 当前 `creators` 同时承担“达人本体”和“项目达人关系”，长期会混乱。
- 一个达人进入多个项目时，难以复用和区分项目状态。
- 缺少全局达人资产层。
- 缺少达人历史数据快照。
- “已合作跟进中”的数据更新记录目前仍偏前端临时展示。

优化建议：

- 新增全局达人表 `creators_global`。
- 新增项目关系表 `project_creators`。
- 新增当前指标表 `creator_metrics_current`。
- 新增历史快照表 `creator_metrics_history`。
- 新增达人数据更新日志 `creator_update_logs`。
- 将项目达人池页面改成读取后端聚合接口。

建议后端聚合接口：

```text
GET /api/projects/{project_id}/creator-pool
GET /api/projects/{project_id}/creator-pool/{creator_id}
POST /api/projects/{project_id}/creator-pool/{creator_id}/update-metrics
PATCH /api/projects/{project_id}/creator-pool/{creator_id}/stage
```

## 4. 采集链路优化

当前状态：

- 有蒲公英采集入口。
- 后端有采集批次表和采集接口。
- 可将采集结果写入本地 DB。

问题：

- 蒲公英后台 RPA 大批量导出能力还不稳定完整。
- 小红书前端关键词筛选还没有实现。
- 采集结果与字段补全的批次链路还不够清晰。
- 采集失败、字段缺失、重复达人处理需要更清楚的 UI 和日志。

优化建议：

- 将采集拆成三个后端任务：
  - 蒲公英后台初筛采集
  - 蒲公英链接详情补全
  - 小红书前端关键词补充筛选
- 每次采集生成 `collection_batch`。
- 每条达人记录保存来源、批次、采集时间和采集字段完整度。
- 前端展示采集批次结果和失败原因。

## 5. 评分与 LLM 匹配优化

当前状态：

- 当前有规则评分。
- 有模型 API 配置。
- 前端展示 ABC 档位。

问题：

- 规则评分和 LLM 匹配分析边界还需要更清晰。
- LLM 输出需要固定结构，避免只给泛泛理由。
- 人工通过/淘汰原因还没有反哺评分规则。
- 项目结构约束如“曝光型 50%、教育垂类或卖货型 50%”还没有真正进入组合优化。

优化建议：

- 规则评分处理硬指标：
  - 报价
  - CPC
  - CPE
  - 35 岁以上粉丝占比
  - 搜索+推荐占比
  - 限流风险
  - 发布时间
- LLM 处理语义匹配：
  - 人设匹配
  - 产品场景匹配
  - 话题讨论度
  - 内容自然度
  - 项目矩阵角色
  - 隐性风险
- LLM 输出固定 JSON。
- 后端根据规则分和 LLM 分计算总分。
- 人工审核结果写入反馈表，后续用于优化标准。

## 6. 人工筛选与流转优化

当前状态：

- 支持单个通过、驳回、备选。
- 支持批量通过、批量淘汰。
- 筛选后从筛选工作台移入项目达人池分组。

问题：

- 状态流转还需要后端统一枚举。
- “通过、备选、淘汰、待建联、合作中”等状态之间的关系需要更明确。
- 缺少状态流转日志的业务展示。

优化建议：

- 定义统一状态机：

```text
待补数据
-> 待审核
-> 合格达人待合作
-> 待建联达人
-> 已合作跟进中
-> 已完成合作
```

异常/淘汰路径：

```text
待审核
-> 观察暂缓
-> 已淘汰归档
```

- 每次状态变化写 `project_creator_stage_logs`。
- 前端达人卡片展示状态历史。

## 7. 飞书联动优化

当前状态：

- 支持保存飞书链接、App ID、App Secret。
- 支持测试连接、读取子表、读取字段。
- 支持写回飞书接口。

问题：

- 飞书仍主要是手动写回，不是稳定双向同步。
- 缺少本地达人与飞书 record_id 的映射。
- 写入策略需要从 append 改为 upsert。
- 飞书人工修改字段没有同步回本地。
- 同步失败重试和冲突策略还不完整。

优化建议：

- 新增 `feishu_sync_state` 表。
- 保存：
  - project_id
  - creator_id
  - feishu_record_id
  - table_id
  - last_synced_at
  - sync_direction
  - sync_status
- 写入飞书使用 upsert：
  - 优先用 record_id 更新
  - 没有 record_id 时用蒲公英链接或达人 ID 查重
  - 查到更新，查不到新增
- 读取飞书人工字段：
  - 负责人
  - 备注
  - 档期
  - 合作状态
  - 建联结果
- 冲突策略：
  - 系统采集字段本地优先
  - 人工协作字段飞书优先
- 同步结果写操作日志。

## 8. 本地文档沉淀优化

当前状态：

- 本地有 CSV/XLSX 示例文件。
- 当前筛选后的达人池主要沉淀在 SQLite DB。

问题：

- 筛选后不会自动更新本地 CSV/XLSX。
- 本地文档还不是稳定快照产物。

优化建议：

- 增加导出接口：

```text
GET /api/projects/{project_id}/exports/creator-pool.csv
GET /api/projects/{project_id}/exports/creator-pool.xlsx
```

- 每次关键节点可生成快照：
  - 初筛完成
  - 人工筛选完成
  - 飞书写回完成
  - 投后复盘完成
- 快照保存到：

```text
runtime/exports/{project_id}/
```

## 9. 投后数据复盘优化

当前状态：

- 文档预期中包含投后数据复盘。
- 当前项目尚未形成完整投后复盘闭环。

问题：

- 缺少合作笔记链接管理。
- 缺少按日抓取投放数据。
- 缺少投后指标快照。
- 缺少评论区情绪和异常识别。

优化建议：

- 新增合作内容表 `campaign_posts`。
- 新增投后数据快照表 `post_performance_snapshots`。
- 支持录入合作笔记链接。
- 定时抓取阅读、点赞、收藏、评论、互动率。
- 计算 CPE、CPC、CPM。
- 支持飞书同步投后表现。

## 10. 媒介建联优化

当前状态：

- 项目达人池中有待建联分组。
- 尚未形成完整媒介工作流。

问题：

- 没有建联记录。
- 没有话术模板。
- 没有达人回复状态。
- 没有底价、返点、寄样地址、排期等商务字段。

优化建议：

- 新增 `creator_outreach_records`。
- 新增 `creator_business_info`。
- 支持建联状态：
  - 待建联
  - 已发送
  - 有意向
  - 无档期
  - 已拒绝
  - 已锁定
- 支持话术模板和备注记录。

## 11. 系统架构优化优先级

### 第一优先级

- 拆分全局达人库和项目达人池。
- 增加历史快照表。
- 修复飞书 upsert 同步。
- 让项目达人池完全从后端聚合接口读取。

### 第二优先级

- 标准化规则评分和 LLM 匹配 JSON。
- 增加评分版本和人工反馈。
- 完善状态机和流转日志。

### 第三优先级

- 实现小红书前端关键词补充筛选。
- 实现蒲公英链接批量详情补全。
- 实现本地 CSV/XLSX 快照导出。

### 第四优先级

- 投后数据复盘。
- 媒介建联工作流。
- 老达人定期巡检。

## 12. 当前结论

当前系统已经具备“单项目筛号工作台”的核心形态。

但要符合《第三事业部-需求优化.docx》的长期目标，还需要把系统从“项目页面”升级为“以达人资产为核心的协同系统”。

最应该优先补的是后端数据模型、飞书双向同步、历史快照和项目达人池后端聚合接口。

