# 多工作台共享模块设计方案

## 目标

当前项目正在从单一达人筛选工作台，升级为多个岗位共同协作的集体工作台。新的系统应以“项目”为核心进程单位，让策划、执行、达人媒介、管理层等角色围绕同一个 `project_id` 协作。

本方案描述多工作台共享模块的设计方向，重点解决：

- 策划工作台的产出如何进入执行工作台。
- 执行工作台的任务进展如何回流项目。
- 达人媒介如何覆盖采集、筛选、建联、交付和数据回流。
- 管理层工作台如何看到各岗位效率、项目健康度和瓶颈。
- 所有工作台如何基于同一个项目上下文协同工作。

## 核心原则

1. 项目是唯一主线。
   所有工作台的数据、任务、日志、产出都必须挂载到 `project_id`。

2. 工作台是岗位视角，不是孤立系统。
   策划、执行、媒介、管理层看到的是同一个项目在不同阶段的切面。

3. 产出必须可交接。
   每个岗位的输出都应该成为下一个岗位的输入，而不是只停留在当前页面。

4. 管理层看结果，也看过程。
   管理层不只看最终数据，还要看到项目进度、协作效率、等待时长、返工情况。

5. 共享模块先服务现有功能，再逐步抽象。
   优先复用达人筛选工作台已经跑通的项目、日志、飞书、达人池能力。

## 项目级主流程

建议完整项目流程为：

```txt
立项 -> 策划产出 -> 执行拆解 -> 媒介/达人协作 -> 交付跟进 -> 数据回流 -> 复盘归档
```

不同工作台在流程中的定位：

| 阶段 | 主要工作台 | 关键产出 |
|---|---|---|
| 立项 | 管理层 / 策划 | 项目基础信息、预算、周期、目标 |
| 策划产出 | 策划工作台 | Brief、策略、内容方向、投放目标 |
| 执行拆解 | 执行工作台 | 任务清单、负责人、排期、交付标准 |
| 达人媒介协作 | 达人媒介工作台 | 达人池、筛选结果、建联状态、合作候选、风险标记 |
| 交付跟进 | 执行工作台 | 交付物、进度、异常、返工 |
| 数据回流 | 执行 / 媒介 / 数据 | 曝光、互动、成本、转化、ROI |
| 复盘归档 | 管理层 / 策划 | 项目复盘、效率分析、经验沉淀 |

## 共享模块总览

建议沉淀以下共享模块：

```txt
shared/
  project/
    ProjectContext
    ProjectHub
    ProjectCard
    ProjectLifecycle
    ProjectSettings

  handoff/
    ProjectHandoff
    HandoffCard
    HandoffStatus

  timeline/
    ProjectTimeline
    ActivityLog

  metrics/
    ProjectMetrics
    RoleEfficiency
    ProjectHealth

  integration/
    IntegrationBinding
    FeishuBinding
    WritebackPanel

  tasks/
    ProjectTaskBoard
    TaskChecklist
    TaskStatus

  assets/
    ProjectAssetPool
    CreatorMedia
    CreatorCollaboration
    CreatorPoolAdapter
```

第一轮不一定需要一次性创建以上目录，可以先在现有 `ad-workbench/src` 中按功能逐步抽出。

## 模块 1：ProjectContext

### 作用

提供全局项目上下文，让所有工作台共享当前项目。

### 核心状态

- `selectedProjectId`
- `currentProject`
- `projects`
- `projectLoading`
- `projectError`

### 核心动作

- `selectProject(projectId)`
- `reloadProjects()`
- `reloadProject(projectId)`
- `createProject(payload)`
- `updateProject(projectId, payload)`
- `archiveProject(projectId)`
- `restoreProject(projectId)`
- `deleteProject(projectId)`

### 复用来源

达人筛选工作台中已有：

- 项目列表加载
- 当前项目选择
- 项目创建
- 项目归档、恢复、删除
- 项目详情刷新

可从 `ScreeningDashboard.jsx` 的项目状态和 handler 中抽出。

## 模块 2：ProjectHub

### 作用

作为完整项目工作台的统一入口，展示所有项目和每个项目的协作进度。

### 页面能力

- 项目列表
- 项目搜索
- 状态筛选
- 归档项目查看
- 项目进度展示
- 进入不同岗位工作台
- 快速查看项目风险和待办

### 复用来源

达人筛选工作台中的：

- `ProjectsPreview`
- `ProjectCard`
- `CreateProjectModal`
- 项目卡片中的状态、周期、预算、目标达人数、进度条

### 后续扩展

项目卡片可以从单一达人筛选进度，升级为多岗位进度：

```txt
策划 100% / 执行 60% / 媒介 40% / 数据 20%
```

## 模块 3：ProjectLifecycle

### 作用

统一描述项目从创建到归档的流程阶段。

### 推荐阶段

```txt
立项
策划中
待执行拆解
执行中
媒介协作中
交付中
数据回流中
复盘中
已归档
```

### 阶段字段建议

- `stage`
- `status`
- `started_at`
- `due_at`
- `completed_at`
- `owner_role`
- `owner_user`
- `blocked_reason`

### 复用来源

达人筛选当前已有专属流程：

```txt
立项 -> 绑定飞书 -> 生成计划 -> 蒲公英采集 -> 初筛评分 -> 人工筛选 -> 项目达人池
```

完整项目中不直接照搬该流程，而是将其作为“达人筛选子流程”挂到项目生命周期中。

## 模块 4：ProjectHandoff

### 作用

解决不同岗位之间“产出可交接”的问题。

### 典型交接

| 来源工作台 | 目标工作台 | 交接内容 |
|---|---|---|
| 策划 | 执行 | Brief、内容策略、核心卖点、KPI、时间节点 |
| 策划 | 达人媒介 | 达人画像、预算、内容方向、筛选标准 |
| 媒介 | 执行 | 已确认达人、合作档期、交付要求 |
| 执行 | 管理层 | 任务进度、延期风险、交付结果 |
| 数据 | 管理层/策划 | 投放数据、ROI、复盘结论 |

### 数据结构建议

```json
{
  "handoff_id": "handoff_xxx",
  "project_id": "project_xxx",
  "from_role": "planner",
  "to_role": "executor",
  "title": "策划方案交接",
  "summary": "本轮投放主打大孩家庭教育场景",
  "payload": {},
  "status": "pending",
  "created_by": "user_xxx",
  "accepted_by": null,
  "created_at": "2026-05-12T10:00:00+08:00",
  "accepted_at": null
}
```

### 状态建议

```txt
draft -> pending -> accepted -> in_progress -> completed
                    -> returned
```

### 前端交互建议

- 交接卡片
- 接收/退回按钮
- 退回原因输入
- 关联任务生成
- 交接记录进入项目动态

## 模块 5：ProjectTimeline

### 作用

统一记录项目中的所有关键动作，成为项目协作时间线。

### 可记录事件

- 项目创建
- 项目信息更新
- 策划方案提交
- 执行任务生成
- 达人池更新
- 飞书绑定
- 飞书写回
- 审核通过/退回
- 交付延期
- 数据回流
- 项目归档

### 复用来源

达人筛选工作台已有：

- `/api/projects/{project_id}/logs`
- `AuditLogTab`
- 后端 `log(...)` 记录机制

### 扩展方向

当前日志偏操作记录，后续可以升级为协作动态：

- 支持角色筛选
- 支持工作台筛选
- 支持关联交接单
- 支持关联任务
- 支持重要事件置顶

## 模块 6：ProjectTaskBoard

### 作用

承接策划产出，把项目目标拆成执行任务。

### 任务字段建议

- `task_id`
- `project_id`
- `source_handoff_id`
- `title`
- `description`
- `role`
- `owner`
- `status`
- `priority`
- `due_at`
- `started_at`
- `completed_at`
- `blocked_reason`
- `deliverables`

### 状态建议

```txt
todo -> doing -> review -> done
              -> blocked
              -> returned
```

### 与执行工作台关系

执行工作台应主要消费 `ProjectTaskBoard`：

- 从策划交接单生成任务。
- 给每个任务分配负责人和截止时间。
- 记录交付物和异常。
- 将任务进度回流项目指标。

## 模块 7：ProjectAssetPool

### 作用

统一管理项目内的资源池，包括达人、素材、文档、交付物、飞书表格等。

### 第一批资源类型

- 统一项目达人池
- 候选达人
- 已合作达人
- Brief 文档
- 策划方案
- 执行任务附件
- 飞书表格
- 数据报表

### 复用来源

达人筛选工作台已有完整的达人池能力：

- `/api/projects/{project_id}/creator-pool`
- 达人阶段切换
- 达人指标更新
- 标签筛选
- 写回飞书
- CSV/XLSX 导出

### 改造方向

达人池不再只是达人筛选工作台的结果页，而是项目资源池的一部分。

媒介和达人筛选必须共用同一套达人记录。达人筛选负责采集、评分、审号和入池；媒介负责建联、报价、档期、合作和投放数据。两者不应各自维护一套达人资产。

媒介工作台可以直接消费：

- 已通过达人
- 备选达人
- 风险达人
- 已合作跟进中达人
- 数据待更新达人

### CreatorCollaboration

`CreatorMedia` 是媒介和达人筛选融合后的共享业务模块。媒介和达人筛选本质上是同一个岗位的连续工作，后续统一称为“达人媒介工作台”。

它统一管理：

- 项目达人池
- 达人筛选结果
- 达人建联状态
- 沟通记录
- 报价和档期
- 发稿和回链
- 投放数据
- 达人风险标签

推荐主状态机：

```txt
候选待采集 -> 已采集待评分 -> 待人工筛选 -> 待审号 -> 已入池待建联 -> 建联中 -> 洽谈中 -> 已确认合作 -> 已排期 -> 已发稿 -> 已回链 -> 已结案
```

任意阶段可进入：

```txt
已淘汰 / 备选 / 风险暂停 / 需复核
```

详细方案见：`达人媒介一体化前后端整合方案.md`。

## 模块 8：IntegrationBinding

### 作用

统一管理项目级外部系统绑定，第一阶段以飞书为主。

### 复用来源

达人筛选工作台已有：

- 飞书连接保存
- 飞书连接测试
- 表格读取
- 字段读取
- 字段映射
- 写回飞书
- 写回设置

### 抽象方向

```txt
IntegrationBinding
  FeishuBinding
  FeishuFieldMapping
  WritebackSettings
  WritebackHistory
```

### 适用工作台

- 策划：写入 Brief 和方案文档。
- 执行：同步任务表和交付状态。
- 媒介：同步达人沟通表。
- 管理层：读取项目报表和效率数据。

## 模块 9：ProjectMetrics

### 作用

为管理层工作台提供项目健康度和岗位效率指标。

### 项目健康度维度

- 项目进度
- 阶段是否延期
- 任务完成率
- 待处理交接数
- 返工次数
- 达人池完成度
- 数据回流完整度
- 风险数量

### 岗位效率维度

| 岗位 | 指标 |
|---|---|
| 策划 | Brief 提交时长、返工次数、方案通过率 |
| 执行 | 任务完成率、延期任务数、平均处理时长 |
| 媒介 | 建联数量、确认率、响应时长、风险达人占比 |
| 达人筛选 | 筛选数量、通过率、备选率、淘汰原因分布 |
| 管理层 | 审批时长、阻塞处理时长 |

### 复用来源

达人筛选已有：

- 项目进度计算
- 达人通过/淘汰/备选/复核状态
- 达人池阶段
- 日志记录
- 采集批次

这些可以先作为管理层看板的第一批数据源。

## 模块 10：RoleWorkspaceShell

### 作用

统一不同工作台的页面外壳，避免每个工作台单独实现项目选择、状态提示、快捷动作。

### 建议包含

- 当前项目选择器
- 项目阶段标签
- 当前岗位待办
- 最近项目动态
- 工作台内部 tab
- 快捷操作区

### 适用页面

- 策划工作台
- 执行工作台
- 媒介工作台
- 达人筛选工作台
- 管理层工作台
- 管理配置工作台

## 数据关系建议

核心关系：

```txt
Project
  -> ProjectStage
  -> Handoff
  -> Task
  -> Asset
  -> Metric
  -> ActivityLog
  -> IntegrationBinding
```

建议所有表或数据文件至少包含：

- `project_id`
- `created_at`
- `updated_at`
- `created_by`
- `updated_by`

涉及岗位协作的数据还应包含：

- `role`
- `owner`
- `status`
- `source`
- `source_id`

## 前端落地顺序

### 第一阶段：项目上下文统一

- 抽出 `ProjectContext`。
- 所有工作台接入同一个当前项目。
- 项目入口统一到 `ProjectHub`。

### 第二阶段：项目动态统一

- 抽出 `ProjectTimeline`。
- 达人筛选日志先接入。
- 策划、执行、媒介动作逐步写入日志。

### 第三阶段：岗位交接打通

- 新增 `ProjectHandoff`。
- 先打通“策划 -> 执行”。
- 再打通“策划 -> 达人筛选”和“达人筛选 -> 媒介”。

### 第四阶段：执行任务板

- 新增 `ProjectTaskBoard`。
- 执行工作台以任务为核心。
- 任务状态回流项目进度。

### 第五阶段：项目资源池

- 将达人池升级为 `ProjectAssetPool` 的一种资源。
- 媒介工作台消费达人池。
- 交付物、文档、数据报表逐步纳入资源池。

### 第六阶段：管理层指标

- 抽出 `ProjectMetrics`。
- 管理层工作台展示项目健康度和岗位效率。
- 支持按项目、岗位、阶段筛选。

## 后端落地顺序

### 第一阶段：沿用现有项目接口

继续使用：

- `/api/projects`
- `/api/projects/{project_id}`
- `/api/projects/{project_id}/logs`
- `/api/projects/{project_id}/creator-pool`
- `/api/projects/feishu/*`

### 第二阶段：新增交接接口

建议接口：

```txt
GET    /api/projects/{project_id}/handoffs
POST   /api/projects/{project_id}/handoffs
PATCH  /api/projects/{project_id}/handoffs/{handoff_id}
POST   /api/projects/{project_id}/handoffs/{handoff_id}/accept
POST   /api/projects/{project_id}/handoffs/{handoff_id}/return
```

### 第三阶段：新增任务接口

建议接口：

```txt
GET    /api/projects/{project_id}/tasks
POST   /api/projects/{project_id}/tasks
PATCH  /api/projects/{project_id}/tasks/{task_id}
POST   /api/projects/{project_id}/tasks/from-handoff/{handoff_id}
```

### 第四阶段：新增指标接口

建议接口：

```txt
GET /api/projects/{project_id}/metrics
GET /api/management/metrics/projects
GET /api/management/metrics/roles
```

## 与 ScreeningDashboard 拆分的关系

`ScreeningDashboard.jsx` 拆分是共享模块建设的前置工作。

拆分完成后，可以优先提升这些模块：

| 从达人筛选拆出的能力 | 提升后的共享模块 |
|---|---|
| 项目列表、项目卡片、项目选择 | `ProjectHub`、`ProjectContext` |
| 项目设置 | `ProjectSettings` |
| 操作日志 | `ProjectTimeline` |
| 飞书绑定 | `IntegrationBinding` |
| 达人池 | `ProjectAssetPool` |
| 项目进度计算 | `ProjectMetrics` |

拆分时不建议直接把所有模块全局化。推荐先完成文件拆分，确认达人筛选功能稳定，再逐步提升到共享层。

## 验收标准

多工作台共享模块第一阶段完成后，应满足：

- 任意工作台都能读取当前项目。
- 切换项目后，各工作台展示同一个项目的数据。
- 策划产出能形成可交接内容。
- 执行工作台能接收策划交接。
- 达人筛选结果能成为项目资源池内容。
- 管理层能看到项目进度、岗位效率和风险。
- 项目动态能串起不同岗位的关键动作。

## 推荐优先级

1. `ProjectContext`
2. `ProjectHub`
3. `ProjectTimeline`
4. `ProjectHandoff`
5. `ProjectTaskBoard`
6. `ProjectAssetPool`
7. `IntegrationBinding`
8. `ProjectMetrics`
9. `RoleWorkspaceShell`

优先处理项目上下文和项目动态，因为它们是多工作台协作的基础。随后再处理交接、任务、资源和管理层指标。
