# 采集自动化脚本优化计划

更新时间：2026年5月15日  
适用范围：KOL/达人数据采集自动化脚本、飞书数据写入链路、分页采集流程、异常恢复机制

## 一、背景判断

当前自动化脚本已经具备完整的采集主链路：

1. 通过触发器启动流程。
2. 获取当前活动标签页。
3. 执行 JavaScript 获取页面上下文与标签页 URL。
4. 判断 URL 是否匹配 KOL 页面。
5. 判断分页数据是否存在。
6. 创建或刷新飞书 token。
7. 查询任务、解析根列表、获取总页数。
8. 按页跳转、等待加载、刷新数据、写入或继续下一页。
9. 在不支持、分页缺失、token 失败、列表为空、加载异常等场景触发通知。

该流程的核心问题不是“有没有采集能力”，而是需要把现有节点式逻辑沉淀为稳定、可复用、可观测、可续跑的采集工程能力。

## 二、优化目标

### 2.1 稳定性目标

- 减少因页面加载慢、分页按钮变化、token 失效导致的采集中断。
- 避免固定延迟不足或过长造成的失败与低效。
- 对异常分支进行统一记录，方便定位失败原因。

### 2.2 完整性目标

- 保证分页数据不漏采、不重复采。
- 支持失败后从指定页或指定任务继续执行。
- 对列表为空、总页数为空、页面结构变化等情况有明确处理策略。

### 2.3 数据质量目标

- 统一 CPM、CPE、粉丝数、报价、互动等字段的采集、清洗和单位转换。
- 建立达人唯一键，避免重复写入。
- 对缺失字段、异常字段、格式变化字段进行标记。

### 2.4 可维护性目标

- 把 token、分页、等待、写入、日志等通用能力抽象成可复用模块。
- 降低后续新增页面类型、字段、数据源时的修改成本。
- 形成自动化脚本版本升级记录和回归检查清单。

## 三、当前流程可学习优化模块

| 模块 | 当前表现 | 优化价值 | 优先级 |
|---|---|---:|---:|
| 飞书 token 创建/刷新 | 已有 `pack-003-fs-token-v3` 节点 | 影响所有写入、查询、任务状态更新 | P0 |
| URL 类型判断 | 已有 `kol` 匹配条件 | 可扩展为多页面类型路由 | P1 |
| 分页数据判断 | 已有“分页数据存在”判断 | 决定是否进入分页采集主流程 | P0 |
| 总页数解析 | 已有 `total page` 节点 | 决定采集边界，直接影响完整性 | P0 |
| 页面跳转 | 已有 `goto current`、`goto next` | 可优化为状态机，避免跳错页 | P0 |
| 页面等待 | 当前包含随机延迟与 `not loading` 判断 | 可优化为条件等待，提高稳定性 | P0 |
| 列表加载判断 | 已有 `.blogger-table...` 判断 | 可用于判断列表是否真正渲染完成 | P0 |
| 数据刷新 | 已有 `refresh` 节点 | 可加入幂等、防抖、失败重试 | P1 |
| 页码累加 | 已有 `page++` | 可与状态持久化结合，支持续跑 | P0 |
| 异常通知 | 已有多个通知节点 | 可统一为错误码、日志、截图、任务状态 | P1 |

## 四、总体优化方案

### 4.1 建立采集状态机

把现有散落节点整理为明确的状态流：

```text
INIT
  -> CHECK_PAGE_TYPE
  -> CHECK_PAGINATION
  -> PREPARE_TOKEN
  -> GET_TOTAL_PAGE
  -> COLLECT_CURRENT_PAGE
  -> WAIT_PAGE_READY
  -> EXTRACT_LIST
  -> WRITE_DATA
  -> NEXT_PAGE
  -> FINISH
```

异常状态：

```text
UNSUPPORTED_PAGE
NO_PAGINATION
TOKEN_FAILED
TOTAL_PAGE_EMPTY
PAGE_LOAD_TIMEOUT
LIST_EMPTY
WRITE_FAILED
TASK_INTERRUPTED
```

每次状态切换都记录：

- task_id
- page_url
- page_type
- current_page
- total_page
- state
- retry_count
- error_code
- error_message
- created_at
- updated_at

### 4.2 优化 token 管理

当前 `pack-003-fs-token-v3` 是关键节点，建议抽象为统一 token 服务。

优化内容：

- token 获取前先检查本地缓存是否有效。
- token 即将过期时提前刷新。
- token 创建失败时重试 2-3 次。
- 区分网络失败、权限失败、配置缺失、接口限流。
- token 失败时记录任务状态，不直接丢失上下文。

建议错误码：

| 错误码 | 含义 | 处理方式 |
|---|---|---|
| TOKEN_CONFIG_MISSING | 飞书配置缺失 | 停止任务并通知 |
| TOKEN_AUTH_FAILED | 授权失败 | 停止任务并通知 |
| TOKEN_NETWORK_ERROR | 网络异常 | 自动重试 |
| TOKEN_RATE_LIMIT | 接口限流 | 延迟后重试 |
| TOKEN_UNKNOWN_ERROR | 未知错误 | 截图、记录上下文 |

### 4.3 优化分页采集逻辑

当前流程已经具备 `total page`、`page > 0`、`goto current`、`goto next`、`page++` 等节点，建议统一为分页状态机。

分页规则：

1. 首次进入页面时解析总页数。
2. 如果总页数为空，进入 `TOTAL_PAGE_EMPTY` 异常。
3. 从 page = 1 开始采集。
4. 每页采集完成后写入当前页状态。
5. 写入成功后再执行 `page++`。
6. 如果当前页大于总页数，任务完成。
7. 如果中途失败，下次从最后成功页的下一页继续。

防漏采策略：

- 每页采集前记录 `page_start`。
- 每页采集后记录 `page_success`。
- 只有数据写入成功后才允许进入下一页。
- 如果页面跳转后实际页码与目标页码不一致，立即重试或停止。

### 4.4 优化页面等待机制

当前使用随机延迟 `randint` 和 `not loading` 判断，可以改成“条件等待优先，随机延迟兜底”。

推荐等待条件：

- loading 元素消失。
- 分页容器存在。
- 表格容器存在。
- 表格行数大于 0。
- 表格行数在 500-1000ms 内保持稳定。
- 当前页码与目标页码一致。

推荐策略：

```text
等待 loading 消失，最多 8 秒
  -> 等待表格容器出现，最多 8 秒
  -> 等待行数稳定，最多 3 秒
  -> 随机延迟 300-900ms
  -> 进入采集
```

超时后处理：

- 第一次超时：刷新当前页面或重新点击当前页。
- 第二次超时：重新获取 token 与任务状态。
- 第三次超时：记录 `PAGE_LOAD_TIMEOUT`，停止当前任务。

### 4.5 优化数据写入与去重

建议为每条达人数据建立唯一键。

优先级：

1. 平台达人 ID。
2. 达人主页 URL。
3. 平台 + 昵称 + 粉丝数快照时间。

写入策略：

- 已存在唯一键：更新最新字段。
- 不存在唯一键：新增记录。
- 关键字段缺失：写入但标记为 `data_warning`。
- 写入失败：保留当前页数据缓存，进入重试。

建议字段：

| 字段 | 说明 |
|---|---|
| kol_id | 平台达人 ID |
| kol_name | 达人名称 |
| profile_url | 达人主页 |
| platform | 平台 |
| fans_count | 粉丝数 |
| cpm | 图文/视频 CPM |
| cpe | 图文/视频 CPE |
| price | 报价 |
| interaction_rate | 互动率 |
| source_url | 采集来源页面 |
| source_page | 来源页码 |
| collected_at | 采集时间 |
| data_status | normal / warning / failed |

### 4.6 优化异常通知

当前通知节点较多，但信息可能不统一。建议统一通知模板。

通知内容：

```text
任务：{task_id}
页面：{page_url}
类型：{page_type}
当前页：{current_page}/{total_page}
状态：{state}
错误码：{error_code}
错误信息：{error_message}
重试次数：{retry_count}
时间：{created_at}
```

异常等级：

| 等级 | 场景 | 动作 |
|---|---|---|
| INFO | 页面不支持、无分页 | 记录即可 |
| WARN | 列表为空、字段缺失 | 记录并继续 |
| ERROR | token 失败、写入失败 | 重试后通知 |
| FATAL | 多次重试失败、页面结构变化 | 停止任务并通知 |

## 五、分阶段执行计划

### 第一阶段：稳定主流程

周期：1-2 天  
目标：让现有脚本更稳，不大改结构。

任务：

- 梳理当前所有节点的输入、输出、失败分支。
- 为 token、分页、加载、写入节点补充统一错误信息。
- 把固定随机延迟改为“条件等待 + 随机兜底”。
- 给每页采集增加页码校验。
- 给分页不存在、总页数为空、列表为空增加明确状态。

交付物：

- 节点清单。
- 错误码清单。
- 主流程回归测试记录。

验收标准：

- 正常 KOL 页面可完整采集所有分页。
- 页面加载慢时不会立即失败。
- token 失败、分页缺失、列表为空能输出明确原因。

### 第二阶段：数据质量与去重

周期：2-3 天  
目标：减少重复、漏写、脏数据。

任务：

- 定义达人唯一键。
- 建立字段清洗规则。
- 支持 CPM/CPE、粉丝数等新增字段。
- 写入飞书前先查询是否已存在。
- 写入后记录页码和采集结果。

交付物：

- 字段映射表。
- 去重规则说明。
- 数据质量检查清单。

验收标准：

- 同一达人重复采集不会产生重复记录。
- 粉丝数、CPM、CPE 能统一格式入库。
- 缺失字段能被标记，不影响整页采集。

### 第三阶段：任务续跑与恢复

周期：2-4 天  
目标：中断后可以恢复，不需要从头跑。

任务：

- 建立任务状态表。
- 每页采集开始、成功、失败都记录状态。
- 支持从指定页继续。
- 支持失败页重试。
- 对连续失败设置停止阈值。

交付物：

- 任务状态字段设计。
- 续跑流程说明。
- 失败恢复测试记录。

验收标准：

- 采集中断后可以从最后成功页继续。
- 单页失败不会导致已采集数据丢失。
- 连续失败达到阈值后自动停止并通知。

### 第四阶段：可配置化与多页面支持

周期：3-5 天  
目标：让脚本支持更多入口和页面结构。

任务：

- 将 URL 匹配规则配置化。
- 将选择器配置化。
- 将字段采集规则配置化。
- 支持 KOL 页面、相似博主搜索列表、搜索结果页等不同页面类型。
- 建立页面类型与采集策略映射。

交付物：

- 页面类型配置表。
- 选择器配置表。
- 多页面采集测试用例。

验收标准：

- 新增页面类型时不需要重写主流程。
- 页面结构小变动时只需调整配置。
- 不支持页面能明确提示，而不是静默失败。

## 六、建议的任务状态表

| 字段 | 类型 | 说明 |
|---|---|---|
| task_id | string | 任务唯一 ID |
| task_name | string | 任务名称 |
| page_url | string | 采集页面 URL |
| page_type | string | 页面类型 |
| status | string | pending / running / success / failed / stopped |
| current_page | number | 当前页 |
| total_page | number | 总页数 |
| success_pages | number | 成功页数 |
| failed_pages | number | 失败页数 |
| retry_count | number | 当前重试次数 |
| last_error_code | string | 最近错误码 |
| last_error_message | string | 最近错误信息 |
| started_at | datetime | 开始时间 |
| updated_at | datetime | 更新时间 |
| finished_at | datetime | 完成时间 |

## 七、建议的错误码清单

| 错误码 | 说明 | 是否重试 |
|---|---|---|
| UNSUPPORTED_PAGE | 当前页面不支持采集 | 否 |
| NO_PAGINATION | 未发现分页组件 | 否 |
| TOTAL_PAGE_EMPTY | 总页数为空 | 否 |
| TOKEN_CREATE_FAILED | token 创建失败 | 是 |
| TOKEN_REFRESH_FAILED | token 刷新失败 | 是 |
| PAGE_LOAD_TIMEOUT | 页面加载超时 | 是 |
| TABLE_NOT_FOUND | 表格容器不存在 | 是 |
| TABLE_EMPTY | 表格为空 | 视场景 |
| PAGE_INDEX_MISMATCH | 实际页码与目标页码不一致 | 是 |
| DATA_EXTRACT_FAILED | 数据解析失败 | 是 |
| DATA_WRITE_FAILED | 数据写入失败 | 是 |
| UNKNOWN_ERROR | 未知异常 | 是 |

## 八、回归测试清单

### 正常场景

- KOL 页面 URL 匹配成功。
- 分页数据存在。
- token 创建成功。
- 总页数获取成功。
- 每页表格加载完成。
- 数据写入成功。
- 页码递增直到任务完成。

### 异常场景

- 非 KOL 页面进入 `UNSUPPORTED_PAGE`。
- 页面无分页进入 `NO_PAGINATION`。
- 分页组件存在但总页数为空进入 `TOTAL_PAGE_EMPTY`。
- token 创建失败后自动重试。
- loading 长时间不消失进入 `PAGE_LOAD_TIMEOUT`。
- 表格容器不存在进入 `TABLE_NOT_FOUND`。
- 写入飞书失败进入 `DATA_WRITE_FAILED`。
- 中途失败后可从最后成功页继续。

### 数据场景

- 同一达人重复采集不会重复写入。
- CPM/CPE 字段为空时有 warning 标记。
- 粉丝数包含“万”“亿”等单位时能正确转换。
- 当前页没有数据时不会错误覆盖历史数据。

## 九、优先落地清单

| 顺序 | 优化项 | 原因 |
|---:|---|---|
| 1 | 条件等待替代纯随机延迟 | 立刻提升稳定性 |
| 2 | 分页状态机 | 防止漏页、重复页、死循环 |
| 3 | token 统一管理 | 降低飞书链路失败率 |
| 4 | 写入去重 | 保护数据质量 |
| 5 | 任务续跑 | 降低中断成本 |
| 6 | 统一错误码和通知 | 提升排查效率 |
| 7 | 选择器和页面规则配置化 | 支撑后续扩展 |

## 十、最终目标形态

优化完成后，该采集自动化脚本应具备以下能力：

- 可识别当前页面是否支持采集。
- 可稳定获取分页、总页数和当前页数据。
- 可自动等待页面真实加载完成。
- 可自动刷新飞书 token。
- 可按唯一键去重写入达人数据。
- 可记录每个任务、每一页、每次失败的状态。
- 可在异常中断后继续采集。
- 可扩展支持更多页面类型和字段。

该项目最值得沉淀的不是单个自动化节点，而是五套通用能力：token 管理、分页状态机、条件等待、数据去重写入、异常恢复。优先把这五套能力做扎实，后续新增 CPM/CPE、粉丝数、相似博主列表、搜索结果页等需求时，改造成本会明显降低。
